#pragma once

#ifndef SCINTILLA_H
#include <Scintilla.h>
#endif

namespace WTL {

class CScintillaLib {
public:
	CScintillaLib() {
		if(++m_nRef==1)
			m_hLib = ::LoadLibrary(TEXT("Scintilla.dll"));
		ATLASSERT(m_hLib && TEXT("Failed to load Scintilla.dll"));
	}
	~CScintillaLib() {
		if(--m_nRef==0) {
			::FreeLibrary(m_hLib);
			m_hLib = NULL;
		}
	}
protected:
	static HMODULE	m_hLib;
	static long		m_nRef;
};

__declspec(selectany) HMODULE	CScintillaLib::m_hLib;
__declspec(selectany) long		CScintillaLib::m_nRef;

template<class TBase>
class CScintillaT : public TBase, public CScintillaLib {
public:
// Constructors
	CScintillaT(HWND hWnd = NULL) : TBase(hWnd) { }

	CScintillaT< TBase >& operator=(HWND hWnd) {
		m_hWnd = hWnd;
		return *this;
	}

	HWND Create(HWND hWndParent, _U_RECT rect = NULL, LPCTSTR szWindowName = NULL,
			DWORD dwStyle = 0, DWORD dwExStyle = 0,
			_U_MENUorID MenuOrID = 0U, LPVOID lpCreateParam = NULL)
	{
		return CWindow::Create(GetWndClassName(), hWndParent, rect.m_lpRect, szWindowName, dwStyle, dwExStyle, MenuOrID.m_hMenu, lpCreateParam);
	}

// Attributes
	static LPCTSTR GetWndClassName()
	{
		return _T("Scintilla");
	}

	/* ++Autogenerated -- start of section automatically generated from Scintilla.iface */

	/// Add text to the document at current position.
	void AddText(int length, LPCSTR text) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_ADDTEXT, (WPARAM)length, (LPARAM)text);
	}

	/// Add array of cells to document.
	void AddStyledText(int length, LPBYTE c) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_ADDSTYLEDTEXT, (WPARAM)length, (LPARAM)c);
	}

	/// Insert string at a position.
	void InsertText(int pos, LPCSTR text) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_INSERTTEXT, (WPARAM)pos, (LPARAM)text);
	}

	/// Change the text that is being inserted in response to SC_MOD_INSERTCHECK
	void ChangeInsertion(int length, LPCSTR text) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_CHANGEINSERTION, (WPARAM)length, (LPARAM)text);
	}

	/// Delete all text in the document.
	void ClearAll() {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_CLEARALL, (WPARAM)0, (LPARAM)0);
	}

	/// Delete a range of text in the document.
	void DeleteRange(int start, int lengthDelete) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_DELETERANGE, (WPARAM)start, (LPARAM)lengthDelete);
	}

	/// Set all style bytes to 0, remove all folding information.
	void ClearDocumentStyle() {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_CLEARDOCUMENTSTYLE, (WPARAM)0, (LPARAM)0);
	}

	/// Returns the number of bytes in the document.
	int GetLength() {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_GETLENGTH, (WPARAM)0, (LPARAM)0);
	}

	/// Returns the character byte at the position.
	int GetCharAt(int pos) {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_GETCHARAT, (WPARAM)pos, (LPARAM)0);
	}

	/// Returns the position of the caret.
	int GetCurrentPos() {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_GETCURRENTPOS, (WPARAM)0, (LPARAM)0);
	}

	/// Returns the position of the opposite end of the selection to the caret.
	int GetAnchor() {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_GETANCHOR, (WPARAM)0, (LPARAM)0);
	}

	/// Returns the style byte at the position.
	int GetStyleAt(int pos) {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_GETSTYLEAT, (WPARAM)pos, (LPARAM)0);
	}

	/// Returns the unsigned style byte at the position.
	int GetStyleIndexAt(int pos) {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_GETSTYLEINDEXAT, (WPARAM)pos, (LPARAM)0);
	}

	/// Redoes the next action on the undo history.
	void Redo() {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_REDO, (WPARAM)0, (LPARAM)0);
	}

	/// Choose between collecting actions into the undo
	/// history and discarding them.
	void SetUndoCollection(bool collectUndo) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SETUNDOCOLLECTION, (WPARAM)collectUndo, (LPARAM)0);
	}

	/// Select all the text in the document.
	void SelectAll() {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SELECTALL, (WPARAM)0, (LPARAM)0);
	}

	/// Remember the current position in the undo history as the position
	/// at which the document was saved.
	void SetSavePoint() {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SETSAVEPOINT, (WPARAM)0, (LPARAM)0);
	}

	/// Retrieve a buffer of cells.
	/// Returns the number of bytes in the buffer not including terminating NULs.
	int GetStyledText(Sci_TextRange* tr) {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_GETSTYLEDTEXT, (WPARAM)0, (LPARAM)tr);
	}

	/// Retrieve a buffer of cells that can be past 2GB.
	/// Returns the number of bytes in the buffer not including terminating NULs.
	int GetStyledTextFull(Sci_TextRangeFull* tr) {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_GETSTYLEDTEXTFULL, (WPARAM)0, (LPARAM)tr);
	}

	/// Are there any redoable actions in the undo history?
	bool CanRedo() {
		ATLASSERT(::IsWindow(m_hWnd));
		return ::SendMessage(m_hWnd, SCI_CANREDO, (WPARAM)0, (LPARAM)0) != 0;
	}

	/// Retrieve the line number at which a particular marker is located.
	int MarkerLineFromHandle(int markerHandle) {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_MARKERLINEFROMHANDLE, (WPARAM)markerHandle, (LPARAM)0);
	}

	/// Delete a marker.
	void MarkerDeleteHandle(int markerHandle) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_MARKERDELETEHANDLE, (WPARAM)markerHandle, (LPARAM)0);
	}

	/// Retrieve marker handles of a line
	int MarkerHandleFromLine(int line, int which) {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_MARKERHANDLEFROMLINE, (WPARAM)line, (LPARAM)which);
	}

	/// Retrieve marker number of a marker handle
	int MarkerNumberFromLine(int line, int which) {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_MARKERNUMBERFROMLINE, (WPARAM)line, (LPARAM)which);
	}

	/// Is undo history being collected?
	bool GetUndoCollection() {
		ATLASSERT(::IsWindow(m_hWnd));
		return ::SendMessage(m_hWnd, SCI_GETUNDOCOLLECTION, (WPARAM)0, (LPARAM)0) != 0;
	}

	/// Are white space characters currently visible?
	/// Returns one of SCWS_* constants.
	int GetViewWS() {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_GETVIEWWS, (WPARAM)0, (LPARAM)0);
	}

	/// Make white space characters invisible, always visible or visible outside indentation.
	void SetViewWS(int viewWS) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SETVIEWWS, (WPARAM)viewWS, (LPARAM)0);
	}

	/// Retrieve the current tab draw mode.
	/// Returns one of SCTD_* constants.
	int GetTabDrawMode() {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_GETTABDRAWMODE, (WPARAM)0, (LPARAM)0);
	}

	/// Set how tabs are drawn when visible.
	void SetTabDrawMode(int tabDrawMode) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SETTABDRAWMODE, (WPARAM)tabDrawMode, (LPARAM)0);
	}

	/// Find the position from a point within the window.
	int PositionFromPoint(int x, int y) {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_POSITIONFROMPOINT, (WPARAM)x, (LPARAM)y);
	}

	/// Find the position from a point within the window but return
	/// INVALID_POSITION if not close to text.
	int PositionFromPointClose(int x, int y) {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_POSITIONFROMPOINTCLOSE, (WPARAM)x, (LPARAM)y);
	}

	/// Set caret to start of a line and ensure it is visible.
	void GotoLine(int line) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_GOTOLINE, (WPARAM)line, (LPARAM)0);
	}

	/// Set caret to a position and ensure it is visible.
	void GotoPos(int caret) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_GOTOPOS, (WPARAM)caret, (LPARAM)0);
	}

	/// Set the selection anchor to a position. The anchor is the opposite
	/// end of the selection from the caret.
	void SetAnchor(int anchor) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SETANCHOR, (WPARAM)anchor, (LPARAM)0);
	}

	/// Retrieve the text of the line containing the caret.
	/// Returns the index of the caret on the line.
	/// Result is NUL-terminated.
	int GetCurLine(int length, LPSTR text) {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_GETCURLINE, (WPARAM)length, (LPARAM)text);
	}

	/// Retrieve the position of the last correctly styled character.
	int GetEndStyled() {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_GETENDSTYLED, (WPARAM)0, (LPARAM)0);
	}

	/// Convert all line endings in the document to one mode.
	void ConvertEOLs(int eolMode) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_CONVERTEOLS, (WPARAM)eolMode, (LPARAM)0);
	}

	/// Retrieve the current end of line mode - one of CRLF, CR, or LF.
	int GetEOLMode() {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_GETEOLMODE, (WPARAM)0, (LPARAM)0);
	}

	/// Set the current end of line mode.
	void SetEOLMode(int eolMode) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SETEOLMODE, (WPARAM)eolMode, (LPARAM)0);
	}

	/// Set the current styling position to start.
	/// The unused parameter is no longer used and should be set to 0.
	void StartStyling(int start, int unused) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_STARTSTYLING, (WPARAM)start, (LPARAM)unused);
	}

	/// Change style from current styling position for length characters to a style
	/// and move the current styling position to after this newly styled segment.
	void SetStyling(int length, int style) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SETSTYLING, (WPARAM)length, (LPARAM)style);
	}

	/// Is drawing done first into a buffer or direct to the screen?
	bool GetBufferedDraw() {
		ATLASSERT(::IsWindow(m_hWnd));
		return ::SendMessage(m_hWnd, SCI_GETBUFFEREDDRAW, (WPARAM)0, (LPARAM)0) != 0;
	}

	/// If drawing is buffered then each line of text is drawn into a bitmap buffer
	/// before drawing it to the screen to avoid flicker.
	void SetBufferedDraw(bool buffered) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SETBUFFEREDDRAW, (WPARAM)buffered, (LPARAM)0);
	}

	/// Change the visible size of a tab to be a multiple of the width of a space character.
	void SetTabWidth(int tabWidth) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SETTABWIDTH, (WPARAM)tabWidth, (LPARAM)0);
	}

	/// Retrieve the visible size of a tab.
	int GetTabWidth() {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_GETTABWIDTH, (WPARAM)0, (LPARAM)0);
	}

	/// Set the minimum visual width of a tab.
	void SetTabMinimumWidth(int pixels) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SETTABMINIMUMWIDTH, (WPARAM)pixels, (LPARAM)0);
	}

	/// Get the minimum visual width of a tab.
	int GetTabMinimumWidth() {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_GETTABMINIMUMWIDTH, (WPARAM)0, (LPARAM)0);
	}

	/// Clear explicit tabstops on a line.
	void ClearTabStops(int line) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_CLEARTABSTOPS, (WPARAM)line, (LPARAM)0);
	}

	/// Add an explicit tab stop for a line.
	void AddTabStop(int line, int x) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_ADDTABSTOP, (WPARAM)line, (LPARAM)x);
	}

	/// Find the next explicit tab stop position on a line after a position.
	int GetNextTabStop(int line, int x) {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_GETNEXTTABSTOP, (WPARAM)line, (LPARAM)x);
	}

	/// Set the code page used to interpret the bytes of the document as characters.
	/// The SC_CP_UTF8 value can be used to enter Unicode mode.
	void SetCodePage(int codePage) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SETCODEPAGE, (WPARAM)codePage, (LPARAM)0);
	}

	/// Set the locale for displaying text.
	void SetFontLocale(LPCSTR localeName) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SETFONTLOCALE, (WPARAM)0, (LPARAM)localeName);
	}

	/// Get the locale for displaying text.
	int GetFontLocale(LPSTR localeName) {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_GETFONTLOCALE, (WPARAM)0, (LPARAM)localeName);
	}

	/// Is the IME displayed in a window or inline?
	int GetIMEInteraction() {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_GETIMEINTERACTION, (WPARAM)0, (LPARAM)0);
	}

	/// Choose to display the IME in a window or inline.
	void SetIMEInteraction(int imeInteraction) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SETIMEINTERACTION, (WPARAM)imeInteraction, (LPARAM)0);
	}

	/// Set the symbol used for a particular marker number.
	void MarkerDefine(int markerNumber, int markerSymbol) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_MARKERDEFINE, (WPARAM)markerNumber, (LPARAM)markerSymbol);
	}

	/// Set the foreground colour used for a particular marker number.
	void MarkerSetFore(int markerNumber, COLORREF fore) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_MARKERSETFORE, (WPARAM)markerNumber, (LPARAM)fore);
	}

	/// Set the background colour used for a particular marker number.
	void MarkerSetBack(int markerNumber, COLORREF back) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_MARKERSETBACK, (WPARAM)markerNumber, (LPARAM)back);
	}

	/// Set the background colour used for a particular marker number when its folding block is selected.
	void MarkerSetBackSelected(int markerNumber, COLORREF back) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_MARKERSETBACKSELECTED, (WPARAM)markerNumber, (LPARAM)back);
	}

	/// Set the foreground colour used for a particular marker number.
	void MarkerSetForeTranslucent(int markerNumber, COLORREF fore) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_MARKERSETFORETRANSLUCENT, (WPARAM)markerNumber, (LPARAM)fore);
	}

	/// Set the background colour used for a particular marker number.
	void MarkerSetBackTranslucent(int markerNumber, COLORREF back) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_MARKERSETBACKTRANSLUCENT, (WPARAM)markerNumber, (LPARAM)back);
	}

	/// Set the background colour used for a particular marker number when its folding block is selected.
	void MarkerSetBackSelectedTranslucent(int markerNumber, COLORREF back) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_MARKERSETBACKSELECTEDTRANSLUCENT, (WPARAM)markerNumber, (LPARAM)back);
	}

	/// Set the width of strokes used in .01 pixels so 50  = 1/2 pixel width.
	void MarkerSetStrokeWidth(int markerNumber, int hundredths) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_MARKERSETSTROKEWIDTH, (WPARAM)markerNumber, (LPARAM)hundredths);
	}

	/// Enable/disable highlight for current folding block (smallest one that contains the caret)
	void MarkerEnableHighlight(bool enabled) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_MARKERENABLEHIGHLIGHT, (WPARAM)enabled, (LPARAM)0);
	}

	/// Add a marker to a line, returning an ID which can be used to find or delete the marker.
	int MarkerAdd(int line, int markerNumber) {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_MARKERADD, (WPARAM)line, (LPARAM)markerNumber);
	}

	/// Delete a marker from a line.
	void MarkerDelete(int line, int markerNumber) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_MARKERDELETE, (WPARAM)line, (LPARAM)markerNumber);
	}

	/// Delete all markers with a particular number from all lines.
	void MarkerDeleteAll(int markerNumber) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_MARKERDELETEALL, (WPARAM)markerNumber, (LPARAM)0);
	}

	/// Get a bit mask of all the markers set on a line.
	int MarkerGet(int line) {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_MARKERGET, (WPARAM)line, (LPARAM)0);
	}

	/// Find the next line at or after lineStart that includes a marker in mask.
	/// Return -1 when no more lines.
	int MarkerNext(int lineStart, int markerMask) {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_MARKERNEXT, (WPARAM)lineStart, (LPARAM)markerMask);
	}

	/// Find the previous line before lineStart that includes a marker in mask.
	int MarkerPrevious(int lineStart, int markerMask) {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_MARKERPREVIOUS, (WPARAM)lineStart, (LPARAM)markerMask);
	}

	/// Define a marker from a pixmap.
	void MarkerDefinePixmap(int markerNumber, LPCSTR pixmap) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_MARKERDEFINEPIXMAP, (WPARAM)markerNumber, (LPARAM)pixmap);
	}

	/// Add a set of markers to a line.
	void MarkerAddSet(int line, int markerSet) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_MARKERADDSET, (WPARAM)line, (LPARAM)markerSet);
	}

	/// Set the alpha used for a marker that is drawn in the text area, not the margin.
	void MarkerSetAlpha(int markerNumber, int alpha) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_MARKERSETALPHA, (WPARAM)markerNumber, (LPARAM)alpha);
	}

	/// Get the layer used for a marker that is drawn in the text area, not the margin.
	int MarkerGetLayer(int markerNumber) {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_MARKERGETLAYER, (WPARAM)markerNumber, (LPARAM)0);
	}

	/// Set the layer used for a marker that is drawn in the text area, not the margin.
	void MarkerSetLayer(int markerNumber, int layer) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_MARKERSETLAYER, (WPARAM)markerNumber, (LPARAM)layer);
	}

	/// Set a margin to be either numeric or symbolic.
	void SetMarginTypeN(int margin, int marginType) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SETMARGINTYPEN, (WPARAM)margin, (LPARAM)marginType);
	}

	/// Retrieve the type of a margin.
	int GetMarginTypeN(int margin) {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_GETMARGINTYPEN, (WPARAM)margin, (LPARAM)0);
	}

	/// Set the width of a margin to a width expressed in pixels.
	void SetMarginWidthN(int margin, int pixelWidth) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SETMARGINWIDTHN, (WPARAM)margin, (LPARAM)pixelWidth);
	}

	/// Retrieve the width of a margin in pixels.
	int GetMarginWidthN(int margin) {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_GETMARGINWIDTHN, (WPARAM)margin, (LPARAM)0);
	}

	/// Set a mask that determines which markers are displayed in a margin.
	void SetMarginMaskN(int margin, int mask) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SETMARGINMASKN, (WPARAM)margin, (LPARAM)mask);
	}

	/// Retrieve the marker mask of a margin.
	int GetMarginMaskN(int margin) {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_GETMARGINMASKN, (WPARAM)margin, (LPARAM)0);
	}

	/// Make a margin sensitive or insensitive to mouse clicks.
	void SetMarginSensitiveN(int margin, bool sensitive) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SETMARGINSENSITIVEN, (WPARAM)margin, (LPARAM)sensitive);
	}

	/// Retrieve the mouse click sensitivity of a margin.
	bool GetMarginSensitiveN(int margin) {
		ATLASSERT(::IsWindow(m_hWnd));
		return ::SendMessage(m_hWnd, SCI_GETMARGINSENSITIVEN, (WPARAM)margin, (LPARAM)0) != 0;
	}

	/// Set the cursor shown when the mouse is inside a margin.
	void SetMarginCursorN(int margin, int cursor) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SETMARGINCURSORN, (WPARAM)margin, (LPARAM)cursor);
	}

	/// Retrieve the cursor shown in a margin.
	int GetMarginCursorN(int margin) {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_GETMARGINCURSORN, (WPARAM)margin, (LPARAM)0);
	}

	/// Set the background colour of a margin. Only visible for SC_MARGIN_COLOUR.
	void SetMarginBackN(int margin, COLORREF back) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SETMARGINBACKN, (WPARAM)margin, (LPARAM)back);
	}

	/// Retrieve the background colour of a margin
	COLORREF GetMarginBackN(int margin) {
		ATLASSERT(::IsWindow(m_hWnd));
		return (COLORREF)::SendMessage(m_hWnd, SCI_GETMARGINBACKN, (WPARAM)margin, (LPARAM)0);
	}

	/// Allocate a non-standard number of margins.
	void SetMargins(int margins) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SETMARGINS, (WPARAM)margins, (LPARAM)0);
	}

	/// How many margins are there?.
	int GetMargins() {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_GETMARGINS, (WPARAM)0, (LPARAM)0);
	}

	/// Clear all the styles and make equivalent to the global default style.
	void StyleClearAll() {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_STYLECLEARALL, (WPARAM)0, (LPARAM)0);
	}

	/// Set the foreground colour of a style.
	void StyleSetFore(int style, COLORREF fore) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_STYLESETFORE, (WPARAM)style, (LPARAM)fore);
	}

	/// Set the background colour of a style.
	void StyleSetBack(int style, COLORREF back) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_STYLESETBACK, (WPARAM)style, (LPARAM)back);
	}

	/// Set a style to be bold or not.
	void StyleSetBold(int style, bool bold) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_STYLESETBOLD, (WPARAM)style, (LPARAM)bold);
	}

	/// Set a style to be italic or not.
	void StyleSetItalic(int style, bool italic) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_STYLESETITALIC, (WPARAM)style, (LPARAM)italic);
	}

	/// Set the size of characters of a style.
	void StyleSetSize(int style, int sizePoints) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_STYLESETSIZE, (WPARAM)style, (LPARAM)sizePoints);
	}

	/// Set the font of a style.
	void StyleSetFont(int style, LPCSTR fontName) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_STYLESETFONT, (WPARAM)style, (LPARAM)fontName);
	}

	/// Set a style to have its end of line filled or not.
	void StyleSetEOLFilled(int style, bool eolFilled) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_STYLESETEOLFILLED, (WPARAM)style, (LPARAM)eolFilled);
	}

	/// Reset the default style to its state at startup
	void StyleResetDefault() {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_STYLERESETDEFAULT, (WPARAM)0, (LPARAM)0);
	}

	/// Set a style to be underlined or not.
	void StyleSetUnderline(int style, bool underline) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_STYLESETUNDERLINE, (WPARAM)style, (LPARAM)underline);
	}

	/// Get the foreground colour of a style.
	COLORREF StyleGetFore(int style) {
		ATLASSERT(::IsWindow(m_hWnd));
		return (COLORREF)::SendMessage(m_hWnd, SCI_STYLEGETFORE, (WPARAM)style, (LPARAM)0);
	}

	/// Get the background colour of a style.
	COLORREF StyleGetBack(int style) {
		ATLASSERT(::IsWindow(m_hWnd));
		return (COLORREF)::SendMessage(m_hWnd, SCI_STYLEGETBACK, (WPARAM)style, (LPARAM)0);
	}

	/// Get is a style bold or not.
	bool StyleGetBold(int style) {
		ATLASSERT(::IsWindow(m_hWnd));
		return ::SendMessage(m_hWnd, SCI_STYLEGETBOLD, (WPARAM)style, (LPARAM)0) != 0;
	}

	/// Get is a style italic or not.
	bool StyleGetItalic(int style) {
		ATLASSERT(::IsWindow(m_hWnd));
		return ::SendMessage(m_hWnd, SCI_STYLEGETITALIC, (WPARAM)style, (LPARAM)0) != 0;
	}

	/// Get the size of characters of a style.
	int StyleGetSize(int style) {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_STYLEGETSIZE, (WPARAM)style, (LPARAM)0);
	}

	/// Get the font of a style.
	/// Returns the length of the fontName
	/// Result is NUL-terminated.
	int StyleGetFont(int style, LPSTR fontName) {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_STYLEGETFONT, (WPARAM)style, (LPARAM)fontName);
	}

	/// Get is a style to have its end of line filled or not.
	bool StyleGetEOLFilled(int style) {
		ATLASSERT(::IsWindow(m_hWnd));
		return ::SendMessage(m_hWnd, SCI_STYLEGETEOLFILLED, (WPARAM)style, (LPARAM)0) != 0;
	}

	/// Get is a style underlined or not.
	bool StyleGetUnderline(int style) {
		ATLASSERT(::IsWindow(m_hWnd));
		return ::SendMessage(m_hWnd, SCI_STYLEGETUNDERLINE, (WPARAM)style, (LPARAM)0) != 0;
	}

	/// Get is a style mixed case, or to force upper or lower case.
	int StyleGetCase(int style) {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_STYLEGETCASE, (WPARAM)style, (LPARAM)0);
	}

	/// Get the character get of the font in a style.
	int StyleGetCharacterSet(int style) {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_STYLEGETCHARACTERSET, (WPARAM)style, (LPARAM)0);
	}

	/// Get is a style visible or not.
	bool StyleGetVisible(int style) {
		ATLASSERT(::IsWindow(m_hWnd));
		return ::SendMessage(m_hWnd, SCI_STYLEGETVISIBLE, (WPARAM)style, (LPARAM)0) != 0;
	}

	/// Get is a style changeable or not (read only).
	/// Experimental feature, currently buggy.
	bool StyleGetChangeable(int style) {
		ATLASSERT(::IsWindow(m_hWnd));
		return ::SendMessage(m_hWnd, SCI_STYLEGETCHANGEABLE, (WPARAM)style, (LPARAM)0) != 0;
	}

	/// Get is a style a hotspot or not.
	bool StyleGetHotSpot(int style) {
		ATLASSERT(::IsWindow(m_hWnd));
		return ::SendMessage(m_hWnd, SCI_STYLEGETHOTSPOT, (WPARAM)style, (LPARAM)0) != 0;
	}

	/// Set a style to be mixed case, or to force upper or lower case.
	void StyleSetCase(int style, int caseVisible) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_STYLESETCASE, (WPARAM)style, (LPARAM)caseVisible);
	}

	/// Set the size of characters of a style. Size is in points multiplied by 100.
	void StyleSetSizeFractional(int style, int sizeHundredthPoints) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_STYLESETSIZEFRACTIONAL, (WPARAM)style, (LPARAM)sizeHundredthPoints);
	}

	/// Get the size of characters of a style in points multiplied by 100
	int StyleGetSizeFractional(int style) {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_STYLEGETSIZEFRACTIONAL, (WPARAM)style, (LPARAM)0);
	}

	/// Set the weight of characters of a style.
	void StyleSetWeight(int style, int weight) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_STYLESETWEIGHT, (WPARAM)style, (LPARAM)weight);
	}

	/// Get the weight of characters of a style.
	int StyleGetWeight(int style) {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_STYLEGETWEIGHT, (WPARAM)style, (LPARAM)0);
	}

	/// Set the character set of the font in a style.
	void StyleSetCharacterSet(int style, int characterSet) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_STYLESETCHARACTERSET, (WPARAM)style, (LPARAM)characterSet);
	}

	/// Set a style to be a hotspot or not.
	void StyleSetHotSpot(int style, bool hotspot) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_STYLESETHOTSPOT, (WPARAM)style, (LPARAM)hotspot);
	}

	/// Indicate that a style may be monospaced over ASCII graphics characters which enables optimizations.
	void StyleSetCheckMonospaced(int style, bool checkMonospaced) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_STYLESETCHECKMONOSPACED, (WPARAM)style, (LPARAM)checkMonospaced);
	}

	/// Get whether a style may be monospaced.
	bool StyleGetCheckMonospaced(int style) {
		ATLASSERT(::IsWindow(m_hWnd));
		return ::SendMessage(m_hWnd, SCI_STYLEGETCHECKMONOSPACED, (WPARAM)style, (LPARAM)0) != 0;
	}

	/// Set the stretch of characters of a style.
	void StyleSetStretch(int style, int stretch) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_STYLESETSTRETCH, (WPARAM)style, (LPARAM)stretch);
	}

	/// Get the stretch of characters of a style.
	int StyleGetStretch(int style) {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_STYLEGETSTRETCH, (WPARAM)style, (LPARAM)0);
	}

	/// Set the invisible representation for a style.
	void StyleSetInvisibleRepresentation(int style, LPCSTR representation) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_STYLESETINVISIBLEREPRESENTATION, (WPARAM)style, (LPARAM)representation);
	}

	/// Get the invisible representation for a style.
	int StyleGetInvisibleRepresentation(int style, LPSTR representation) {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_STYLEGETINVISIBLEREPRESENTATION, (WPARAM)style, (LPARAM)representation);
	}

	/// Set the colour of an element. Translucency (alpha) may or may not be significant
	/// and this may depend on the platform. The alpha byte should commonly be 0xff for opaque.
	void SetElementColour(int element, COLORREF colourElement) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SETELEMENTCOLOUR, (WPARAM)element, (LPARAM)colourElement);
	}

	/// Get the colour of an element.
	COLORREF GetElementColour(int element) {
		ATLASSERT(::IsWindow(m_hWnd));
		return (COLORREF)::SendMessage(m_hWnd, SCI_GETELEMENTCOLOUR, (WPARAM)element, (LPARAM)0);
	}

	/// Use the default or platform-defined colour for an element.
	void ResetElementColour(int element) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_RESETELEMENTCOLOUR, (WPARAM)element, (LPARAM)0);
	}

	/// Get whether an element has been set by SetElementColour.
	/// When false, a platform-defined or default colour is used.
	bool GetElementIsSet(int element) {
		ATLASSERT(::IsWindow(m_hWnd));
		return ::SendMessage(m_hWnd, SCI_GETELEMENTISSET, (WPARAM)element, (LPARAM)0) != 0;
	}

	/// Get whether an element supports translucency.
	bool GetElementAllowsTranslucent(int element) {
		ATLASSERT(::IsWindow(m_hWnd));
		return ::SendMessage(m_hWnd, SCI_GETELEMENTALLOWSTRANSLUCENT, (WPARAM)element, (LPARAM)0) != 0;
	}

	/// Get the colour of an element.
	COLORREF GetElementBaseColour(int element) {
		ATLASSERT(::IsWindow(m_hWnd));
		return (COLORREF)::SendMessage(m_hWnd, SCI_GETELEMENTBASECOLOUR, (WPARAM)element, (LPARAM)0);
	}

	/// Set the foreground colour of the main and additional selections and whether to use this setting.
	void SetSelFore(bool useSetting, COLORREF fore) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SETSELFORE, (WPARAM)useSetting, (LPARAM)fore);
	}

	/// Set the background colour of the main and additional selections and whether to use this setting.
	void SetSelBack(bool useSetting, COLORREF back) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SETSELBACK, (WPARAM)useSetting, (LPARAM)back);
	}

	/// Get the alpha of the selection.
	int GetSelAlpha() {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_GETSELALPHA, (WPARAM)0, (LPARAM)0);
	}

	/// Set the alpha of the selection.
	void SetSelAlpha(int alpha) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SETSELALPHA, (WPARAM)alpha, (LPARAM)0);
	}

	/// Is the selection end of line filled?
	bool GetSelEOLFilled() {
		ATLASSERT(::IsWindow(m_hWnd));
		return ::SendMessage(m_hWnd, SCI_GETSELEOLFILLED, (WPARAM)0, (LPARAM)0) != 0;
	}

	/// Set the selection to have its end of line filled or not.
	void SetSelEOLFilled(bool filled) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SETSELEOLFILLED, (WPARAM)filled, (LPARAM)0);
	}

	/// Get the layer for drawing selections
	int GetSelectionLayer() {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_GETSELECTIONLAYER, (WPARAM)0, (LPARAM)0);
	}

	/// Set the layer for drawing selections: either opaquely on base layer or translucently over text
	void SetSelectionLayer(int layer) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SETSELECTIONLAYER, (WPARAM)layer, (LPARAM)0);
	}

	/// Get the layer of the background of the line containing the caret.
	int GetCaretLineLayer() {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_GETCARETLINELAYER, (WPARAM)0, (LPARAM)0);
	}

	/// Set the layer of the background of the line containing the caret.
	void SetCaretLineLayer(int layer) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SETCARETLINELAYER, (WPARAM)layer, (LPARAM)0);
	}

	/// Get only highlighting subline instead of whole line.
	bool GetCaretLineHighlightSubLine() {
		ATLASSERT(::IsWindow(m_hWnd));
		return ::SendMessage(m_hWnd, SCI_GETCARETLINEHIGHLIGHTSUBLINE, (WPARAM)0, (LPARAM)0) != 0;
	}

	/// Set only highlighting subline instead of whole line.
	void SetCaretLineHighlightSubLine(bool subLine) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SETCARETLINEHIGHLIGHTSUBLINE, (WPARAM)subLine, (LPARAM)0);
	}

	/// Set the foreground colour of the caret.
	void SetCaretFore(COLORREF fore) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SETCARETFORE, (WPARAM)fore, (LPARAM)0);
	}

	/// When key+modifier combination keyDefinition is pressed perform sciCommand.
	void AssignCmdKey(int keyDefinition, int sciCommand) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_ASSIGNCMDKEY, (WPARAM)keyDefinition, (LPARAM)sciCommand);
	}

	/// When key+modifier combination keyDefinition is pressed do nothing.
	void ClearCmdKey(int keyDefinition) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_CLEARCMDKEY, (WPARAM)keyDefinition, (LPARAM)0);
	}

	/// Drop all key mappings.
	void ClearAllCmdKeys() {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_CLEARALLCMDKEYS, (WPARAM)0, (LPARAM)0);
	}

	/// Reset all key mappings.
	void ResetAllCmdKeys(int cmdKeys) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_RESETALLCMDKEYS, (WPARAM)cmdKeys, (LPARAM)0);
	}

	/// Set the styles for a segment of the document.
	void SetStylingEx(int length, LPCSTR styles) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SETSTYLINGEX, (WPARAM)length, (LPARAM)styles);
	}

	/// Set a style to be visible or not.
	void StyleSetVisible(int style, bool visible) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_STYLESETVISIBLE, (WPARAM)style, (LPARAM)visible);
	}

	/// Get the time in milliseconds that the caret is on and off.
	int GetCaretPeriod() {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_GETCARETPERIOD, (WPARAM)0, (LPARAM)0);
	}

	/// Get the time in milliseconds that the caret is on and off. 0 = steady on.
	void SetCaretPeriod(int periodMilliseconds) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SETCARETPERIOD, (WPARAM)periodMilliseconds, (LPARAM)0);
	}

	/// Set the set of characters making up words for when moving or selecting by word.
	/// First sets defaults like SetCharsDefault.
	void SetWordChars(LPCSTR characters) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SETWORDCHARS, (WPARAM)0, (LPARAM)characters);
	}

	/// Get the set of characters making up words for when moving or selecting by word.
	/// Returns the number of characters
	int GetWordChars(LPSTR characters) {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_GETWORDCHARS, (WPARAM)0, (LPARAM)characters);
	}

	/// Set the number of characters to have directly indexed categories
	void SetCharacterCategoryOptimization(int countCharacters) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SETCHARACTERCATEGORYOPTIMIZATION, (WPARAM)countCharacters, (LPARAM)0);
	}

	/// Get the number of characters to have directly indexed categories
	int GetCharacterCategoryOptimization() {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_GETCHARACTERCATEGORYOPTIMIZATION, (WPARAM)0, (LPARAM)0);
	}

	/// Start a sequence of actions that is undone and redone as a unit.
	/// May be nested.
	void BeginUndoAction() {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_BEGINUNDOACTION, (WPARAM)0, (LPARAM)0);
	}

	/// End a sequence of actions that is undone and redone as a unit.
	void EndUndoAction() {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_ENDUNDOACTION, (WPARAM)0, (LPARAM)0);
	}

	/// Is an undo sequence active?
	int GetUndoSequence() {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_GETUNDOSEQUENCE, (WPARAM)0, (LPARAM)0);
	}

	/// How many undo actions are in the history?
	int GetUndoActions() {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_GETUNDOACTIONS, (WPARAM)0, (LPARAM)0);
	}

	/// Set action as the save point
	void SetUndoSavePoint(int action) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SETUNDOSAVEPOINT, (WPARAM)action, (LPARAM)0);
	}

	/// Which action is the save point?
	int GetUndoSavePoint() {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_GETUNDOSAVEPOINT, (WPARAM)0, (LPARAM)0);
	}

	/// Set action as the detach point
	void SetUndoDetach(int action) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SETUNDODETACH, (WPARAM)action, (LPARAM)0);
	}

	/// Which action is the detach point?
	int GetUndoDetach() {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_GETUNDODETACH, (WPARAM)0, (LPARAM)0);
	}

	/// Set action as the tentative point
	void SetUndoTentative(int action) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SETUNDOTENTATIVE, (WPARAM)action, (LPARAM)0);
	}

	/// Which action is the tentative point?
	int GetUndoTentative() {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_GETUNDOTENTATIVE, (WPARAM)0, (LPARAM)0);
	}

	/// Set action as the current point
	void SetUndoCurrent(int action) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SETUNDOCURRENT, (WPARAM)action, (LPARAM)0);
	}

	/// Which action is the current point?
	int GetUndoCurrent() {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_GETUNDOCURRENT, (WPARAM)0, (LPARAM)0);
	}

	/// Push one action onto undo history with no text
	void PushUndoActionType(int type, int pos) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_PUSHUNDOACTIONTYPE, (WPARAM)type, (LPARAM)pos);
	}

	/// Set the text and length of the most recently pushed action
	void ChangeLastUndoActionText(int length, LPCSTR text) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_CHANGELASTUNDOACTIONTEXT, (WPARAM)length, (LPARAM)text);
	}

	/// What is the type of an action?
	int GetUndoActionType(int action) {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_GETUNDOACTIONTYPE, (WPARAM)action, (LPARAM)0);
	}

	/// What is the position of an action?
	int GetUndoActionPosition(int action) {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_GETUNDOACTIONPOSITION, (WPARAM)action, (LPARAM)0);
	}

	/// What is the text of an action?
	int GetUndoActionText(int action, LPSTR text) {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_GETUNDOACTIONTEXT, (WPARAM)action, (LPARAM)text);
	}

	/// Set an indicator to plain, squiggle or TT.
	void IndicSetStyle(int indicator, int indicatorStyle) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_INDICSETSTYLE, (WPARAM)indicator, (LPARAM)indicatorStyle);
	}

	/// Retrieve the style of an indicator.
	int IndicGetStyle(int indicator) {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_INDICGETSTYLE, (WPARAM)indicator, (LPARAM)0);
	}

	/// Set the foreground colour of an indicator.
	void IndicSetFore(int indicator, COLORREF fore) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_INDICSETFORE, (WPARAM)indicator, (LPARAM)fore);
	}

	/// Retrieve the foreground colour of an indicator.
	COLORREF IndicGetFore(int indicator) {
		ATLASSERT(::IsWindow(m_hWnd));
		return (COLORREF)::SendMessage(m_hWnd, SCI_INDICGETFORE, (WPARAM)indicator, (LPARAM)0);
	}

	/// Set an indicator to draw under text or over(default).
	void IndicSetUnder(int indicator, bool under) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_INDICSETUNDER, (WPARAM)indicator, (LPARAM)under);
	}

	/// Retrieve whether indicator drawn under or over text.
	bool IndicGetUnder(int indicator) {
		ATLASSERT(::IsWindow(m_hWnd));
		return ::SendMessage(m_hWnd, SCI_INDICGETUNDER, (WPARAM)indicator, (LPARAM)0) != 0;
	}

	/// Set a hover indicator to plain, squiggle or TT.
	void IndicSetHoverStyle(int indicator, int indicatorStyle) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_INDICSETHOVERSTYLE, (WPARAM)indicator, (LPARAM)indicatorStyle);
	}

	/// Retrieve the hover style of an indicator.
	int IndicGetHoverStyle(int indicator) {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_INDICGETHOVERSTYLE, (WPARAM)indicator, (LPARAM)0);
	}

	/// Set the foreground hover colour of an indicator.
	void IndicSetHoverFore(int indicator, COLORREF fore) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_INDICSETHOVERFORE, (WPARAM)indicator, (LPARAM)fore);
	}

	/// Retrieve the foreground hover colour of an indicator.
	COLORREF IndicGetHoverFore(int indicator) {
		ATLASSERT(::IsWindow(m_hWnd));
		return (COLORREF)::SendMessage(m_hWnd, SCI_INDICGETHOVERFORE, (WPARAM)indicator, (LPARAM)0);
	}

	/// Set the attributes of an indicator.
	void IndicSetFlags(int indicator, int flags) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_INDICSETFLAGS, (WPARAM)indicator, (LPARAM)flags);
	}

	/// Retrieve the attributes of an indicator.
	int IndicGetFlags(int indicator) {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_INDICGETFLAGS, (WPARAM)indicator, (LPARAM)0);
	}

	/// Set the stroke width of an indicator in hundredths of a pixel.
	void IndicSetStrokeWidth(int indicator, int hundredths) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_INDICSETSTROKEWIDTH, (WPARAM)indicator, (LPARAM)hundredths);
	}

	/// Retrieve the stroke width of an indicator.
	int IndicGetStrokeWidth(int indicator) {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_INDICGETSTROKEWIDTH, (WPARAM)indicator, (LPARAM)0);
	}

	/// Set the foreground colour of all whitespace and whether to use this setting.
	void SetWhitespaceFore(bool useSetting, COLORREF fore) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SETWHITESPACEFORE, (WPARAM)useSetting, (LPARAM)fore);
	}

	/// Set the background colour of all whitespace and whether to use this setting.
	void SetWhitespaceBack(bool useSetting, COLORREF back) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SETWHITESPACEBACK, (WPARAM)useSetting, (LPARAM)back);
	}

	/// Set the size of the dots used to mark space characters.
	void SetWhitespaceSize(int size) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SETWHITESPACESIZE, (WPARAM)size, (LPARAM)0);
	}

	/// Get the size of the dots used to mark space characters.
	int GetWhitespaceSize() {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_GETWHITESPACESIZE, (WPARAM)0, (LPARAM)0);
	}

	/// Used to hold extra styling information for each line.
	void SetLineState(int line, int state) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SETLINESTATE, (WPARAM)line, (LPARAM)state);
	}

	/// Retrieve the extra styling information for a line.
	int GetLineState(int line) {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_GETLINESTATE, (WPARAM)line, (LPARAM)0);
	}

	/// Retrieve the last line number that has line state.
	int GetMaxLineState() {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_GETMAXLINESTATE, (WPARAM)0, (LPARAM)0);
	}

	/// Is the background of the line containing the caret in a different colour?
	bool GetCaretLineVisible() {
		ATLASSERT(::IsWindow(m_hWnd));
		return ::SendMessage(m_hWnd, SCI_GETCARETLINEVISIBLE, (WPARAM)0, (LPARAM)0) != 0;
	}

	/// Display the background of the line containing the caret in a different colour.
	void SetCaretLineVisible(bool show) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SETCARETLINEVISIBLE, (WPARAM)show, (LPARAM)0);
	}

	/// Get the colour of the background of the line containing the caret.
	COLORREF GetCaretLineBack() {
		ATLASSERT(::IsWindow(m_hWnd));
		return (COLORREF)::SendMessage(m_hWnd, SCI_GETCARETLINEBACK, (WPARAM)0, (LPARAM)0);
	}

	/// Set the colour of the background of the line containing the caret.
	void SetCaretLineBack(COLORREF back) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SETCARETLINEBACK, (WPARAM)back, (LPARAM)0);
	}

	/// Retrieve the caret line frame width.
	/// Width = 0 means this option is disabled.
	int GetCaretLineFrame() {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_GETCARETLINEFRAME, (WPARAM)0, (LPARAM)0);
	}

	/// Display the caret line framed.
	/// Set width != 0 to enable this option and width = 0 to disable it.
	void SetCaretLineFrame(int width) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SETCARETLINEFRAME, (WPARAM)width, (LPARAM)0);
	}

	/// Set a style to be changeable or not (read only).
	/// Experimental feature, currently buggy.
	void StyleSetChangeable(int style, bool changeable) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_STYLESETCHANGEABLE, (WPARAM)style, (LPARAM)changeable);
	}

	/// Display a auto-completion list.
	/// The lengthEntered parameter indicates how many characters before
	/// the caret should be used to provide context.
	void AutoCShow(int lengthEntered, LPCSTR itemList) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_AUTOCSHOW, (WPARAM)lengthEntered, (LPARAM)itemList);
	}

	/// Remove the auto-completion list from the screen.
	void AutoCCancel() {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_AUTOCCANCEL, (WPARAM)0, (LPARAM)0);
	}

	/// Is there an auto-completion list visible?
	bool AutoCActive() {
		ATLASSERT(::IsWindow(m_hWnd));
		return ::SendMessage(m_hWnd, SCI_AUTOCACTIVE, (WPARAM)0, (LPARAM)0) != 0;
	}

	/// Retrieve the position of the caret when the auto-completion list was displayed.
	int AutoCPosStart() {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_AUTOCPOSSTART, (WPARAM)0, (LPARAM)0);
	}

	/// User has selected an item so remove the list and insert the selection.
	void AutoCComplete() {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_AUTOCCOMPLETE, (WPARAM)0, (LPARAM)0);
	}

	/// Define a set of character that when typed cancel the auto-completion list.
	void AutoCStops(LPCSTR characterSet) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_AUTOCSTOPS, (WPARAM)0, (LPARAM)characterSet);
	}

	/// Change the separator character in the string setting up an auto-completion list.
	/// Default is space but can be changed if items contain space.
	void AutoCSetSeparator(int separatorCharacter) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_AUTOCSETSEPARATOR, (WPARAM)separatorCharacter, (LPARAM)0);
	}

	/// Retrieve the auto-completion list separator character.
	int AutoCGetSeparator() {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_AUTOCGETSEPARATOR, (WPARAM)0, (LPARAM)0);
	}

	/// Select the item in the auto-completion list that starts with a string.
	void AutoCSelect(LPCSTR select) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_AUTOCSELECT, (WPARAM)0, (LPARAM)select);
	}

	/// Should the auto-completion list be cancelled if the user backspaces to a
	/// position before where the box was created.
	void AutoCSetCancelAtStart(bool cancel) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_AUTOCSETCANCELATSTART, (WPARAM)cancel, (LPARAM)0);
	}

	/// Retrieve whether auto-completion cancelled by backspacing before start.
	bool AutoCGetCancelAtStart() {
		ATLASSERT(::IsWindow(m_hWnd));
		return ::SendMessage(m_hWnd, SCI_AUTOCGETCANCELATSTART, (WPARAM)0, (LPARAM)0) != 0;
	}

	/// Define a set of characters that when typed will cause the autocompletion to
	/// choose the selected item.
	void AutoCSetFillUps(LPCSTR characterSet) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_AUTOCSETFILLUPS, (WPARAM)0, (LPARAM)characterSet);
	}

	/// Should a single item auto-completion list automatically choose the item.
	void AutoCSetChooseSingle(bool chooseSingle) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_AUTOCSETCHOOSESINGLE, (WPARAM)chooseSingle, (LPARAM)0);
	}

	/// Retrieve whether a single item auto-completion list automatically choose the item.
	bool AutoCGetChooseSingle() {
		ATLASSERT(::IsWindow(m_hWnd));
		return ::SendMessage(m_hWnd, SCI_AUTOCGETCHOOSESINGLE, (WPARAM)0, (LPARAM)0) != 0;
	}

	/// Set whether case is significant when performing auto-completion searches.
	void AutoCSetIgnoreCase(bool ignoreCase) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_AUTOCSETIGNORECASE, (WPARAM)ignoreCase, (LPARAM)0);
	}

	/// Retrieve state of ignore case flag.
	bool AutoCGetIgnoreCase() {
		ATLASSERT(::IsWindow(m_hWnd));
		return ::SendMessage(m_hWnd, SCI_AUTOCGETIGNORECASE, (WPARAM)0, (LPARAM)0) != 0;
	}

	/// Display a list of strings and send notification when user chooses one.
	void UserListShow(int listType, LPCSTR itemList) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_USERLISTSHOW, (WPARAM)listType, (LPARAM)itemList);
	}

	/// Set whether or not autocompletion is hidden automatically when nothing matches.
	void AutoCSetAutoHide(bool autoHide) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_AUTOCSETAUTOHIDE, (WPARAM)autoHide, (LPARAM)0);
	}

	/// Retrieve whether or not autocompletion is hidden automatically when nothing matches.
	bool AutoCGetAutoHide() {
		ATLASSERT(::IsWindow(m_hWnd));
		return ::SendMessage(m_hWnd, SCI_AUTOCGETAUTOHIDE, (WPARAM)0, (LPARAM)0) != 0;
	}

	/// Set autocompletion options.
	void AutoCSetOptions(int options) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_AUTOCSETOPTIONS, (WPARAM)options, (LPARAM)0);
	}

	/// Retrieve autocompletion options.
	int AutoCGetOptions() {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_AUTOCGETOPTIONS, (WPARAM)0, (LPARAM)0);
	}

	/// Set whether or not autocompletion deletes any word characters
	/// after the inserted text upon completion.
	void AutoCSetDropRestOfWord(bool dropRestOfWord) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_AUTOCSETDROPRESTOFWORD, (WPARAM)dropRestOfWord, (LPARAM)0);
	}

	/// Retrieve whether or not autocompletion deletes any word characters
	/// after the inserted text upon completion.
	bool AutoCGetDropRestOfWord() {
		ATLASSERT(::IsWindow(m_hWnd));
		return ::SendMessage(m_hWnd, SCI_AUTOCGETDROPRESTOFWORD, (WPARAM)0, (LPARAM)0) != 0;
	}

	/// Register an XPM image for use in autocompletion lists.
	void RegisterImage(int type, LPCSTR xpmData) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_REGISTERIMAGE, (WPARAM)type, (LPARAM)xpmData);
	}

	/// Clear all the registered XPM images.
	void ClearRegisteredImages() {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_CLEARREGISTEREDIMAGES, (WPARAM)0, (LPARAM)0);
	}

	/// Retrieve the auto-completion list type-separator character.
	int AutoCGetTypeSeparator() {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_AUTOCGETTYPESEPARATOR, (WPARAM)0, (LPARAM)0);
	}

	/// Change the type-separator character in the string setting up an auto-completion list.
	/// Default is '?' but can be changed if items contain '?'.
	void AutoCSetTypeSeparator(int separatorCharacter) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_AUTOCSETTYPESEPARATOR, (WPARAM)separatorCharacter, (LPARAM)0);
	}

	/// Set the maximum width, in characters, of auto-completion and user lists.
	/// Set to 0 to autosize to fit longest item, which is the default.
	void AutoCSetMaxWidth(int characterCount) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_AUTOCSETMAXWIDTH, (WPARAM)characterCount, (LPARAM)0);
	}

	/// Get the maximum width, in characters, of auto-completion and user lists.
	int AutoCGetMaxWidth() {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_AUTOCGETMAXWIDTH, (WPARAM)0, (LPARAM)0);
	}

	/// Set the minimum width, in characters, of auto-completion and user lists.
	/// Set to 0 to autosize to fit longest item, which is the default.
	void AutoCSetMinWidth(int characterCount) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_AUTOCSETMINWIDTH, (WPARAM)characterCount, (LPARAM)0);
	}

	/// Get the minimum width, in characters, of auto-completion and user lists.
	int AutoCGetMinWidth() {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_AUTOCGETMINWIDTH, (WPARAM)0, (LPARAM)0);
	}

	/// Set the maximum height, in rows, of auto-completion and user lists.
	/// The default is 5 rows.
	void AutoCSetMaxHeight(int rowCount) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_AUTOCSETMAXHEIGHT, (WPARAM)rowCount, (LPARAM)0);
	}

	/// Set the maximum height, in rows, of auto-completion and user lists.
	int AutoCGetMaxHeight() {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_AUTOCGETMAXHEIGHT, (WPARAM)0, (LPARAM)0);
	}

	/// Set the style number used for auto-completion and user lists fonts.
	void AutoCSetStyle(int style) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_AUTOCSETSTYLE, (WPARAM)style, (LPARAM)0);
	}

	/// Get the style number used for auto-completion and user lists fonts.
	int AutoCGetStyle() {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_AUTOCGETSTYLE, (WPARAM)0, (LPARAM)0);
	}

	/// Set the number of spaces used for one level of indentation.
	void SetIndent(int indentSize) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SETINDENT, (WPARAM)indentSize, (LPARAM)0);
	}

	/// Retrieve indentation size.
	int GetIndent() {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_GETINDENT, (WPARAM)0, (LPARAM)0);
	}

	/// Indentation will only use space characters if useTabs is false, otherwise
	/// it will use a combination of tabs and spaces.
	void SetUseTabs(bool useTabs) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SETUSETABS, (WPARAM)useTabs, (LPARAM)0);
	}

	/// Retrieve whether tabs will be used in indentation.
	bool GetUseTabs() {
		ATLASSERT(::IsWindow(m_hWnd));
		return ::SendMessage(m_hWnd, SCI_GETUSETABS, (WPARAM)0, (LPARAM)0) != 0;
	}

	/// Change the indentation of a line to a number of columns.
	void SetLineIndentation(int line, int indentation) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SETLINEINDENTATION, (WPARAM)line, (LPARAM)indentation);
	}

	/// Retrieve the number of columns that a line is indented.
	int GetLineIndentation(int line) {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_GETLINEINDENTATION, (WPARAM)line, (LPARAM)0);
	}

	/// Retrieve the position before the first non indentation character on a line.
	int GetLineIndentPosition(int line) {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_GETLINEINDENTPOSITION, (WPARAM)line, (LPARAM)0);
	}

	/// Retrieve the column number of a position, taking tab width into account.
	int GetColumn(int pos) {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_GETCOLUMN, (WPARAM)pos, (LPARAM)0);
	}

	/// Count characters between two positions.
	int CountCharacters(int start, int end) {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_COUNTCHARACTERS, (WPARAM)start, (LPARAM)end);
	}

	/// Count code units between two positions.
	int CountCodeUnits(int start, int end) {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_COUNTCODEUNITS, (WPARAM)start, (LPARAM)end);
	}

	/// Show or hide the horizontal scroll bar.
	void SetHScrollBar(bool visible) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SETHSCROLLBAR, (WPARAM)visible, (LPARAM)0);
	}

	/// Is the horizontal scroll bar visible?
	bool GetHScrollBar() {
		ATLASSERT(::IsWindow(m_hWnd));
		return ::SendMessage(m_hWnd, SCI_GETHSCROLLBAR, (WPARAM)0, (LPARAM)0) != 0;
	}

	/// Show or hide indentation guides.
	void SetIndentationGuides(int indentView) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SETINDENTATIONGUIDES, (WPARAM)indentView, (LPARAM)0);
	}

	/// Are the indentation guides visible?
	int GetIndentationGuides() {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_GETINDENTATIONGUIDES, (WPARAM)0, (LPARAM)0);
	}

	/// Set the highlighted indentation guide column.
	/// 0 = no highlighted guide.
	void SetHighlightGuide(int column) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SETHIGHLIGHTGUIDE, (WPARAM)column, (LPARAM)0);
	}

	/// Get the highlighted indentation guide column.
	int GetHighlightGuide() {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_GETHIGHLIGHTGUIDE, (WPARAM)0, (LPARAM)0);
	}

	/// Get the position after the last visible characters on a line.
	int GetLineEndPosition(int line) {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_GETLINEENDPOSITION, (WPARAM)line, (LPARAM)0);
	}

	/// Get the code page used to interpret the bytes of the document as characters.
	int GetCodePage() {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_GETCODEPAGE, (WPARAM)0, (LPARAM)0);
	}

	/// Get the foreground colour of the caret.
	COLORREF GetCaretFore() {
		ATLASSERT(::IsWindow(m_hWnd));
		return (COLORREF)::SendMessage(m_hWnd, SCI_GETCARETFORE, (WPARAM)0, (LPARAM)0);
	}

	/// In read-only mode?
	bool GetReadOnly() {
		ATLASSERT(::IsWindow(m_hWnd));
		return ::SendMessage(m_hWnd, SCI_GETREADONLY, (WPARAM)0, (LPARAM)0) != 0;
	}

	/// Sets the position of the caret.
	void SetCurrentPos(int caret) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SETCURRENTPOS, (WPARAM)caret, (LPARAM)0);
	}

	/// Sets the position that starts the selection - this becomes the anchor.
	void SetSelectionStart(int anchor) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SETSELECTIONSTART, (WPARAM)anchor, (LPARAM)0);
	}

	/// Returns the position at the start of the selection.
	int GetSelectionStart() {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_GETSELECTIONSTART, (WPARAM)0, (LPARAM)0);
	}

	/// Sets the position that ends the selection - this becomes the caret.
	void SetSelectionEnd(int caret) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SETSELECTIONEND, (WPARAM)caret, (LPARAM)0);
	}

	/// Returns the position at the end of the selection.
	int GetSelectionEnd() {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_GETSELECTIONEND, (WPARAM)0, (LPARAM)0);
	}

	/// Set caret to a position, while removing any existing selection.
	void SetEmptySelection(int caret) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SETEMPTYSELECTION, (WPARAM)caret, (LPARAM)0);
	}

	/// Sets the print magnification added to the point size of each style for printing.
	void SetPrintMagnification(int magnification) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SETPRINTMAGNIFICATION, (WPARAM)magnification, (LPARAM)0);
	}

	/// Returns the print magnification.
	int GetPrintMagnification() {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_GETPRINTMAGNIFICATION, (WPARAM)0, (LPARAM)0);
	}

	/// Modify colours when printing for clearer printed text.
	void SetPrintColourMode(int mode) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SETPRINTCOLOURMODE, (WPARAM)mode, (LPARAM)0);
	}

	/// Returns the print colour mode.
	int GetPrintColourMode() {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_GETPRINTCOLOURMODE, (WPARAM)0, (LPARAM)0);
	}

	/// Find some text in the document.
	int FindText(int searchFlags, Sci_TextToFind* ft) {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_FINDTEXT, (WPARAM)searchFlags, (LPARAM)ft);
	}

	/// Find some text in the document.
	int FindTextFull(int searchFlags, Sci_TextToFindFull* ft) {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_FINDTEXTFULL, (WPARAM)searchFlags, (LPARAM)ft);
	}

	/// Draw the document into a display context such as a printer.
	int FormatRange(bool draw, Sci_RangeToFormat* fr) {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_FORMATRANGE, (WPARAM)draw, (LPARAM)fr);
	}

	/// Draw the document into a display context such as a printer.
	int FormatRangeFull(bool draw, Sci_RangeToFormatFull* fr) {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_FORMATRANGEFULL, (WPARAM)draw, (LPARAM)fr);
	}

	/// Enable or disable change history.
	void SetChangeHistory(int changeHistory) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SETCHANGEHISTORY, (WPARAM)changeHistory, (LPARAM)0);
	}

	/// Report change history status.
	int GetChangeHistory() {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_GETCHANGEHISTORY, (WPARAM)0, (LPARAM)0);
	}

	/// Enable or disable undo selection history.
	void SetUndoSelectionHistory(int undoSelectionHistory) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SETUNDOSELECTIONHISTORY, (WPARAM)undoSelectionHistory, (LPARAM)0);
	}

	/// Report undo selection history status.
	int GetUndoSelectionHistory() {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_GETUNDOSELECTIONHISTORY, (WPARAM)0, (LPARAM)0);
	}

	/// Set selection from serialized form.
	void SetSelectionSerialized(LPCSTR selectionString) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SETSELECTIONSERIALIZED, (WPARAM)0, (LPARAM)selectionString);
	}

	/// Retrieve serialized form of selection.
	int GetSelectionSerialized(LPSTR selectionString) {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_GETSELECTIONSERIALIZED, (WPARAM)0, (LPARAM)selectionString);
	}

	/// Retrieve the display line at the top of the display.
	int GetFirstVisibleLine() {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_GETFIRSTVISIBLELINE, (WPARAM)0, (LPARAM)0);
	}

	/// Retrieve the contents of a line.
	/// Returns the length of the line.
	int GetLine(int line, LPSTR text) {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_GETLINE, (WPARAM)line, (LPARAM)text);
	}

	/// Returns the number of lines in the document. There is always at least one.
	int GetLineCount() {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_GETLINECOUNT, (WPARAM)0, (LPARAM)0);
	}

	/// Enlarge the number of lines allocated.
	void AllocateLines(int lines) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_ALLOCATELINES, (WPARAM)lines, (LPARAM)0);
	}

	/// Sets the size in pixels of the left margin.
	void SetMarginLeft(int pixelWidth) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SETMARGINLEFT, (WPARAM)0, (LPARAM)pixelWidth);
	}

	/// Returns the size in pixels of the left margin.
	int GetMarginLeft() {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_GETMARGINLEFT, (WPARAM)0, (LPARAM)0);
	}

	/// Sets the size in pixels of the right margin.
	void SetMarginRight(int pixelWidth) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SETMARGINRIGHT, (WPARAM)0, (LPARAM)pixelWidth);
	}

	/// Returns the size in pixels of the right margin.
	int GetMarginRight() {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_GETMARGINRIGHT, (WPARAM)0, (LPARAM)0);
	}

	/// Is the document different from when it was last saved?
	bool GetModify() {
		ATLASSERT(::IsWindow(m_hWnd));
		return ::SendMessage(m_hWnd, SCI_GETMODIFY, (WPARAM)0, (LPARAM)0) != 0;
	}

	/// Select a range of text.
	void SetSel(int anchor, int caret) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SETSEL, (WPARAM)anchor, (LPARAM)caret);
	}

	/// Retrieve the selected text.
	/// Return the length of the text.
	/// Result is NUL-terminated.
	int GetSelText(LPSTR text) {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_GETSELTEXT, (WPARAM)0, (LPARAM)text);
	}

	/// Retrieve a range of text.
	/// Return the length of the text.
	int GetTextRange(Sci_TextRange* tr) {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_GETTEXTRANGE, (WPARAM)0, (LPARAM)tr);
	}

	/// Retrieve a range of text that can be past 2GB.
	/// Return the length of the text.
	int GetTextRangeFull(Sci_TextRangeFull* tr) {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_GETTEXTRANGEFULL, (WPARAM)0, (LPARAM)tr);
	}

	/// Draw the selection either highlighted or in normal (non-highlighted) style.
	void HideSelection(bool hide) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_HIDESELECTION, (WPARAM)hide, (LPARAM)0);
	}

	/// GetSelectionHidden: auto-generated function.
	bool GetSelectionHidden() {
		ATLASSERT(::IsWindow(m_hWnd));
		return ::SendMessage(m_hWnd, SCI_GETSELECTIONHIDDEN, (WPARAM)0, (LPARAM)0) != 0;
	}

	/// Retrieve the x value of the point in the window where a position is displayed.
	int PointXFromPosition(int pos) {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_POINTXFROMPOSITION, (WPARAM)0, (LPARAM)pos);
	}

	/// Retrieve the y value of the point in the window where a position is displayed.
	int PointYFromPosition(int pos) {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_POINTYFROMPOSITION, (WPARAM)0, (LPARAM)pos);
	}

	/// Retrieve the line containing a position.
	int LineFromPosition(int pos) {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_LINEFROMPOSITION, (WPARAM)pos, (LPARAM)0);
	}

	/// Retrieve the position at the start of a line.
	int PositionFromLine(int line) {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_POSITIONFROMLINE, (WPARAM)line, (LPARAM)0);
	}

	/// Scroll horizontally and vertically.
	void LineScroll(int columns, int lines) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_LINESCROLL, (WPARAM)columns, (LPARAM)lines);
	}

	/// Ensure the caret is visible.
	void ScrollCaret() {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SCROLLCARET, (WPARAM)0, (LPARAM)0);
	}

	/// Scroll the argument positions and the range between them into view giving
	/// priority to the primary position then the secondary position.
	/// This may be used to make a search match visible.
	void ScrollRange(int secondary, int primary) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SCROLLRANGE, (WPARAM)secondary, (LPARAM)primary);
	}

	/// Replace the selected text with the argument text.
	void ReplaceSel(LPCSTR text) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_REPLACESEL, (WPARAM)0, (LPARAM)text);
	}

	/// Set to read only or read write.
	void SetReadOnly(bool readOnly) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SETREADONLY, (WPARAM)readOnly, (LPARAM)0);
	}

	/// Null operation.
	void Null() {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_NULL, (WPARAM)0, (LPARAM)0);
	}

	/// Will a paste succeed?
	bool CanPaste() {
		ATLASSERT(::IsWindow(m_hWnd));
		return ::SendMessage(m_hWnd, SCI_CANPASTE, (WPARAM)0, (LPARAM)0) != 0;
	}

	/// Are there any undoable actions in the undo history?
	bool CanUndo() {
		ATLASSERT(::IsWindow(m_hWnd));
		return ::SendMessage(m_hWnd, SCI_CANUNDO, (WPARAM)0, (LPARAM)0) != 0;
	}

	/// Delete the undo history.
	void EmptyUndoBuffer() {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_EMPTYUNDOBUFFER, (WPARAM)0, (LPARAM)0);
	}

	/// Undo one action in the undo history.
	void Undo() {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_UNDO, (WPARAM)0, (LPARAM)0);
	}

	/// Cut the selection to the clipboard.
	void Cut() {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_CUT, (WPARAM)0, (LPARAM)0);
	}

	/// Copy the selection to the clipboard.
	void Copy() {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_COPY, (WPARAM)0, (LPARAM)0);
	}

	/// Paste the contents of the clipboard into the document replacing the selection.
	void Paste() {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_PASTE, (WPARAM)0, (LPARAM)0);
	}

	/// Clear the selection.
	void Clear() {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_CLEAR, (WPARAM)0, (LPARAM)0);
	}

	/// Replace the contents of the document with the argument text.
	void SetText(LPCSTR text) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SETTEXT, (WPARAM)0, (LPARAM)text);
	}

	/// Retrieve all the text in the document.
	/// Returns number of characters retrieved.
	/// Result is NUL-terminated.
	int GetText(int length, LPSTR text) {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_GETTEXT, (WPARAM)length, (LPARAM)text);
	}

	/// Retrieve the number of characters in the document.
	int GetTextLength() {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_GETTEXTLENGTH, (WPARAM)0, (LPARAM)0);
	}

	/// Retrieve a pointer to a function that processes messages for this Scintilla.
	void* GetDirectFunction() {
		ATLASSERT(::IsWindow(m_hWnd));
		return (void*)::SendMessage(m_hWnd, SCI_GETDIRECTFUNCTION, (WPARAM)0, (LPARAM)0);
	}

	/// Retrieve a pointer to a function that processes messages for this Scintilla and returns status.
	void* GetDirectStatusFunction() {
		ATLASSERT(::IsWindow(m_hWnd));
		return (void*)::SendMessage(m_hWnd, SCI_GETDIRECTSTATUSFUNCTION, (WPARAM)0, (LPARAM)0);
	}

	/// Retrieve a pointer value to use as the first argument when calling
	/// the function returned by GetDirectFunction.
	void* GetDirectPointer() {
		ATLASSERT(::IsWindow(m_hWnd));
		return (void*)::SendMessage(m_hWnd, SCI_GETDIRECTPOINTER, (WPARAM)0, (LPARAM)0);
	}

	/// Set to overtype (true) or insert mode.
	void SetOvertype(bool overType) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SETOVERTYPE, (WPARAM)overType, (LPARAM)0);
	}

	/// Returns true if overtype mode is active otherwise false is returned.
	bool GetOvertype() {
		ATLASSERT(::IsWindow(m_hWnd));
		return ::SendMessage(m_hWnd, SCI_GETOVERTYPE, (WPARAM)0, (LPARAM)0) != 0;
	}

	/// Set the width of the insert mode caret.
	void SetCaretWidth(int pixelWidth) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SETCARETWIDTH, (WPARAM)pixelWidth, (LPARAM)0);
	}

	/// Returns the width of the insert mode caret.
	int GetCaretWidth() {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_GETCARETWIDTH, (WPARAM)0, (LPARAM)0);
	}

	/// Sets the position that starts the target which is used for updating the
	/// document without affecting the scroll position.
	void SetTargetStart(int start) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SETTARGETSTART, (WPARAM)start, (LPARAM)0);
	}

	/// Get the position that starts the target.
	int GetTargetStart() {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_GETTARGETSTART, (WPARAM)0, (LPARAM)0);
	}

	/// Sets the virtual space of the target start
	void SetTargetStartVirtualSpace(int space) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SETTARGETSTARTVIRTUALSPACE, (WPARAM)space, (LPARAM)0);
	}

	/// Get the virtual space of the target start
	int GetTargetStartVirtualSpace() {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_GETTARGETSTARTVIRTUALSPACE, (WPARAM)0, (LPARAM)0);
	}

	/// Sets the position that ends the target which is used for updating the
	/// document without affecting the scroll position.
	void SetTargetEnd(int end) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SETTARGETEND, (WPARAM)end, (LPARAM)0);
	}

	/// Get the position that ends the target.
	int GetTargetEnd() {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_GETTARGETEND, (WPARAM)0, (LPARAM)0);
	}

	/// Sets the virtual space of the target end
	void SetTargetEndVirtualSpace(int space) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SETTARGETENDVIRTUALSPACE, (WPARAM)space, (LPARAM)0);
	}

	/// Get the virtual space of the target end
	int GetTargetEndVirtualSpace() {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_GETTARGETENDVIRTUALSPACE, (WPARAM)0, (LPARAM)0);
	}

	/// Sets both the start and end of the target in one call.
	void SetTargetRange(int start, int end) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SETTARGETRANGE, (WPARAM)start, (LPARAM)end);
	}

	/// Retrieve the text in the target.
	int GetTargetText(LPSTR text) {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_GETTARGETTEXT, (WPARAM)0, (LPARAM)text);
	}

	/// Make the target range start and end be the same as the selection range start and end.
	void TargetFromSelection() {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_TARGETFROMSELECTION, (WPARAM)0, (LPARAM)0);
	}

	/// Sets the target to the whole document.
	void TargetWholeDocument() {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_TARGETWHOLEDOCUMENT, (WPARAM)0, (LPARAM)0);
	}

	/// Replace the target text with the argument text.
	/// Text is counted so it can contain NULs.
	/// Returns the length of the replacement text.
	int ReplaceTarget(int length, LPCSTR text) {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_REPLACETARGET, (WPARAM)length, (LPARAM)text);
	}

	/// Replace the target text with the argument text after \d processing.
	/// Text is counted so it can contain NULs.
	/// Looks for \d where d is between 1 and 9 and replaces these with the strings
	/// matched in the last search operation which were surrounded by \( and \).
	/// Returns the length of the replacement text including any change
	/// caused by processing the \d patterns.
	int ReplaceTargetRE(int length, LPCSTR text) {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_REPLACETARGETRE, (WPARAM)length, (LPARAM)text);
	}

	/// Replace the target text with the argument text but ignore prefix and suffix that
	/// are the same as current.
	int ReplaceTargetMinimal(int length, LPCSTR text) {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_REPLACETARGETMINIMAL, (WPARAM)length, (LPARAM)text);
	}

	/// Search for a counted string in the target and set the target to the found
	/// range. Text is counted so it can contain NULs.
	/// Returns start of found range or -1 for failure in which case target is not moved.
	int SearchInTarget(int length, LPCSTR text) {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_SEARCHINTARGET, (WPARAM)length, (LPARAM)text);
	}

	/// Set the search flags used by SearchInTarget.
	void SetSearchFlags(int searchFlags) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SETSEARCHFLAGS, (WPARAM)searchFlags, (LPARAM)0);
	}

	/// Get the search flags used by SearchInTarget.
	int GetSearchFlags() {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_GETSEARCHFLAGS, (WPARAM)0, (LPARAM)0);
	}

	/// Show a call tip containing a definition near position pos.
	void CallTipShow(int pos, LPCSTR definition) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_CALLTIPSHOW, (WPARAM)pos, (LPARAM)definition);
	}

	/// Remove the call tip from the screen.
	void CallTipCancel() {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_CALLTIPCANCEL, (WPARAM)0, (LPARAM)0);
	}

	/// Is there an active call tip?
	bool CallTipActive() {
		ATLASSERT(::IsWindow(m_hWnd));
		return ::SendMessage(m_hWnd, SCI_CALLTIPACTIVE, (WPARAM)0, (LPARAM)0) != 0;
	}

	/// Retrieve the position where the caret was before displaying the call tip.
	int CallTipPosStart() {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_CALLTIPPOSSTART, (WPARAM)0, (LPARAM)0);
	}

	/// Set the start position in order to change when backspacing removes the calltip.
	void CallTipSetPosStart(int posStart) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_CALLTIPSETPOSSTART, (WPARAM)posStart, (LPARAM)0);
	}

	/// Highlight a segment of the definition.
	void CallTipSetHlt(int highlightStart, int highlightEnd) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_CALLTIPSETHLT, (WPARAM)highlightStart, (LPARAM)highlightEnd);
	}

	/// Set the background colour for the call tip.
	void CallTipSetBack(COLORREF back) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_CALLTIPSETBACK, (WPARAM)back, (LPARAM)0);
	}

	/// Set the foreground colour for the call tip.
	void CallTipSetFore(COLORREF fore) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_CALLTIPSETFORE, (WPARAM)fore, (LPARAM)0);
	}

	/// Set the foreground colour for the highlighted part of the call tip.
	void CallTipSetForeHlt(COLORREF fore) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_CALLTIPSETFOREHLT, (WPARAM)fore, (LPARAM)0);
	}

	/// Set the foreground colours for the border of the call tip.
	void CallTipSetForeBorder(COLORREF foreLight, COLORREF foreShade) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_CALLTIPSETFOREBORDER, (WPARAM)foreLight, (LPARAM)foreShade);
	}

	/// Enable use of STYLE_CALLTIP and set call tip tab size in pixels.
	void CallTipUseStyle(int tabSize) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_CALLTIPUSESTYLE, (WPARAM)tabSize, (LPARAM)0);
	}

	/// Set position of calltip, above or below text.
	void CallTipSetPosition(bool above) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_CALLTIPSETPOSITION, (WPARAM)above, (LPARAM)0);
	}

	/// Find the display line of a document line taking hidden lines into account.
	int VisibleFromDocLine(int docLine) {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_VISIBLEFROMDOCLINE, (WPARAM)docLine, (LPARAM)0);
	}

	/// Find the document line of a display line taking hidden lines into account.
	int DocLineFromVisible(int displayLine) {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_DOCLINEFROMVISIBLE, (WPARAM)displayLine, (LPARAM)0);
	}

	/// The number of display lines needed to wrap a document line
	int WrapCount(int docLine) {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_WRAPCOUNT, (WPARAM)docLine, (LPARAM)0);
	}

	/// Set the fold level of a line.
	/// This encodes an integer level along with flags indicating whether the
	/// line is a header and whether it is effectively white space.
	void SetFoldLevel(int line, int level) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SETFOLDLEVEL, (WPARAM)line, (LPARAM)level);
	}

	/// Retrieve the fold level of a line.
	int GetFoldLevel(int line) {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_GETFOLDLEVEL, (WPARAM)line, (LPARAM)0);
	}

	/// Find the last child line of a header line.
	int GetLastChild(int line, int level) {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_GETLASTCHILD, (WPARAM)line, (LPARAM)level);
	}

	/// Find the parent line of a child line.
	int GetFoldParent(int line) {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_GETFOLDPARENT, (WPARAM)line, (LPARAM)0);
	}

	/// Make a range of lines visible.
	void ShowLines(int lineStart, int lineEnd) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SHOWLINES, (WPARAM)lineStart, (LPARAM)lineEnd);
	}

	/// Make a range of lines invisible.
	void HideLines(int lineStart, int lineEnd) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_HIDELINES, (WPARAM)lineStart, (LPARAM)lineEnd);
	}

	/// Is a line visible?
	bool GetLineVisible(int line) {
		ATLASSERT(::IsWindow(m_hWnd));
		return ::SendMessage(m_hWnd, SCI_GETLINEVISIBLE, (WPARAM)line, (LPARAM)0) != 0;
	}

	/// Are all lines visible?
	bool GetAllLinesVisible() {
		ATLASSERT(::IsWindow(m_hWnd));
		return ::SendMessage(m_hWnd, SCI_GETALLLINESVISIBLE, (WPARAM)0, (LPARAM)0) != 0;
	}

	/// Show the children of a header line.
	void SetFoldExpanded(int line, bool expanded) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SETFOLDEXPANDED, (WPARAM)line, (LPARAM)expanded);
	}

	/// Is a header line expanded?
	bool GetFoldExpanded(int line) {
		ATLASSERT(::IsWindow(m_hWnd));
		return ::SendMessage(m_hWnd, SCI_GETFOLDEXPANDED, (WPARAM)line, (LPARAM)0) != 0;
	}

	/// Switch a header line between expanded and contracted.
	void ToggleFold(int line) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_TOGGLEFOLD, (WPARAM)line, (LPARAM)0);
	}

	/// Switch a header line between expanded and contracted and show some text after the line.
	void ToggleFoldShowText(int line, LPCSTR text) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_TOGGLEFOLDSHOWTEXT, (WPARAM)line, (LPARAM)text);
	}

	/// Set the style of fold display text.
	void FoldDisplayTextSetStyle(int style) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_FOLDDISPLAYTEXTSETSTYLE, (WPARAM)style, (LPARAM)0);
	}

	/// Get the style of fold display text.
	int FoldDisplayTextGetStyle() {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_FOLDDISPLAYTEXTGETSTYLE, (WPARAM)0, (LPARAM)0);
	}

	/// Set the default fold display text.
	void SetDefaultFoldDisplayText(LPCSTR text) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SETDEFAULTFOLDDISPLAYTEXT, (WPARAM)0, (LPARAM)text);
	}

	/// Get the default fold display text.
	int GetDefaultFoldDisplayText(LPSTR text) {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_GETDEFAULTFOLDDISPLAYTEXT, (WPARAM)0, (LPARAM)text);
	}

	/// Expand or contract a fold header.
	void FoldLine(int line, int action) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_FOLDLINE, (WPARAM)line, (LPARAM)action);
	}

	/// Expand or contract a fold header and its children.
	void FoldChildren(int line, int action) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_FOLDCHILDREN, (WPARAM)line, (LPARAM)action);
	}

	/// Expand a fold header and all children. Use the level argument instead of the line's current level.
	void ExpandChildren(int line, int level) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_EXPANDCHILDREN, (WPARAM)line, (LPARAM)level);
	}

	/// Expand or contract all fold headers.
	void FoldAll(int action) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_FOLDALL, (WPARAM)action, (LPARAM)0);
	}

	/// Ensure a particular line is visible by expanding any header line hiding it.
	void EnsureVisible(int line) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_ENSUREVISIBLE, (WPARAM)line, (LPARAM)0);
	}

	/// Set automatic folding behaviours.
	void SetAutomaticFold(int automaticFold) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SETAUTOMATICFOLD, (WPARAM)automaticFold, (LPARAM)0);
	}

	/// Get automatic folding behaviours.
	int GetAutomaticFold() {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_GETAUTOMATICFOLD, (WPARAM)0, (LPARAM)0);
	}

	/// Set some style options for folding.
	void SetFoldFlags(int flags) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SETFOLDFLAGS, (WPARAM)flags, (LPARAM)0);
	}

	/// Ensure a particular line is visible by expanding any header line hiding it.
	/// Use the currently set visibility policy to determine which range to display.
	void EnsureVisibleEnforcePolicy(int line) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_ENSUREVISIBLEENFORCEPOLICY, (WPARAM)line, (LPARAM)0);
	}

	/// Sets whether a tab pressed when caret is within indentation indents.
	void SetTabIndents(bool tabIndents) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SETTABINDENTS, (WPARAM)tabIndents, (LPARAM)0);
	}

	/// Does a tab pressed when caret is within indentation indent?
	bool GetTabIndents() {
		ATLASSERT(::IsWindow(m_hWnd));
		return ::SendMessage(m_hWnd, SCI_GETTABINDENTS, (WPARAM)0, (LPARAM)0) != 0;
	}

	/// Sets whether a backspace pressed when caret is within indentation unindents.
	void SetBackSpaceUnIndents(bool bsUnIndents) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SETBACKSPACEUNINDENTS, (WPARAM)bsUnIndents, (LPARAM)0);
	}

	/// Does a backspace pressed when caret is within indentation unindent?
	bool GetBackSpaceUnIndents() {
		ATLASSERT(::IsWindow(m_hWnd));
		return ::SendMessage(m_hWnd, SCI_GETBACKSPACEUNINDENTS, (WPARAM)0, (LPARAM)0) != 0;
	}

	/// Sets the time the mouse must sit still to generate a mouse dwell event.
	void SetMouseDwellTime(int periodMilliseconds) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SETMOUSEDWELLTIME, (WPARAM)periodMilliseconds, (LPARAM)0);
	}

	/// Retrieve the time the mouse must sit still to generate a mouse dwell event.
	int GetMouseDwellTime() {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_GETMOUSEDWELLTIME, (WPARAM)0, (LPARAM)0);
	}

	/// Get position of start of word.
	int WordStartPosition(int pos, bool onlyWordCharacters) {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_WORDSTARTPOSITION, (WPARAM)pos, (LPARAM)onlyWordCharacters);
	}

	/// Get position of end of word.
	int WordEndPosition(int pos, bool onlyWordCharacters) {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_WORDENDPOSITION, (WPARAM)pos, (LPARAM)onlyWordCharacters);
	}

	/// Is the range start..end considered a word?
	bool IsRangeWord(int start, int end) {
		ATLASSERT(::IsWindow(m_hWnd));
		return ::SendMessage(m_hWnd, SCI_ISRANGEWORD, (WPARAM)start, (LPARAM)end) != 0;
	}

	/// Sets limits to idle styling.
	void SetIdleStyling(int idleStyling) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SETIDLESTYLING, (WPARAM)idleStyling, (LPARAM)0);
	}

	/// Retrieve the limits to idle styling.
	int GetIdleStyling() {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_GETIDLESTYLING, (WPARAM)0, (LPARAM)0);
	}

	/// Sets whether text is word wrapped.
	void SetWrapMode(int wrapMode) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SETWRAPMODE, (WPARAM)wrapMode, (LPARAM)0);
	}

	/// Retrieve whether text is word wrapped.
	int GetWrapMode() {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_GETWRAPMODE, (WPARAM)0, (LPARAM)0);
	}

	/// Set the display mode of visual flags for wrapped lines.
	void SetWrapVisualFlags(int wrapVisualFlags) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SETWRAPVISUALFLAGS, (WPARAM)wrapVisualFlags, (LPARAM)0);
	}

	/// Retrive the display mode of visual flags for wrapped lines.
	int GetWrapVisualFlags() {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_GETWRAPVISUALFLAGS, (WPARAM)0, (LPARAM)0);
	}

	/// Set the location of visual flags for wrapped lines.
	void SetWrapVisualFlagsLocation(int wrapVisualFlagsLocation) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SETWRAPVISUALFLAGSLOCATION, (WPARAM)wrapVisualFlagsLocation, (LPARAM)0);
	}

	/// Retrive the location of visual flags for wrapped lines.
	int GetWrapVisualFlagsLocation() {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_GETWRAPVISUALFLAGSLOCATION, (WPARAM)0, (LPARAM)0);
	}

	/// Set the start indent for wrapped lines.
	void SetWrapStartIndent(int indent) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SETWRAPSTARTINDENT, (WPARAM)indent, (LPARAM)0);
	}

	/// Retrive the start indent for wrapped lines.
	int GetWrapStartIndent() {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_GETWRAPSTARTINDENT, (WPARAM)0, (LPARAM)0);
	}

	/// Sets how wrapped sublines are placed. Default is fixed.
	void SetWrapIndentMode(int wrapIndentMode) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SETWRAPINDENTMODE, (WPARAM)wrapIndentMode, (LPARAM)0);
	}

	/// Retrieve how wrapped sublines are placed. Default is fixed.
	int GetWrapIndentMode() {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_GETWRAPINDENTMODE, (WPARAM)0, (LPARAM)0);
	}

	/// Sets the degree of caching of layout information.
	void SetLayoutCache(int cacheMode) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SETLAYOUTCACHE, (WPARAM)cacheMode, (LPARAM)0);
	}

	/// Retrieve the degree of caching of layout information.
	int GetLayoutCache() {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_GETLAYOUTCACHE, (WPARAM)0, (LPARAM)0);
	}

	/// Sets the document width assumed for scrolling.
	void SetScrollWidth(int pixelWidth) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SETSCROLLWIDTH, (WPARAM)pixelWidth, (LPARAM)0);
	}

	/// Retrieve the document width assumed for scrolling.
	int GetScrollWidth() {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_GETSCROLLWIDTH, (WPARAM)0, (LPARAM)0);
	}

	/// Sets whether the maximum width line displayed is used to set scroll width.
	void SetScrollWidthTracking(bool tracking) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SETSCROLLWIDTHTRACKING, (WPARAM)tracking, (LPARAM)0);
	}

	/// Retrieve whether the scroll width tracks wide lines.
	bool GetScrollWidthTracking() {
		ATLASSERT(::IsWindow(m_hWnd));
		return ::SendMessage(m_hWnd, SCI_GETSCROLLWIDTHTRACKING, (WPARAM)0, (LPARAM)0) != 0;
	}

	/// Measure the pixel width of some text in a particular style.
	/// NUL terminated text argument.
	/// Does not handle tab or control characters.
	int TextWidth(int style, LPCSTR text) {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_TEXTWIDTH, (WPARAM)style, (LPARAM)text);
	}

	/// Sets the scroll range so that maximum scroll position has
	/// the last line at the bottom of the view (default).
	/// Setting this to false allows scrolling one page below the last line.
	void SetEndAtLastLine(bool endAtLastLine) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SETENDATLASTLINE, (WPARAM)endAtLastLine, (LPARAM)0);
	}

	/// Retrieve whether the maximum scroll position has the last
	/// line at the bottom of the view.
	bool GetEndAtLastLine() {
		ATLASSERT(::IsWindow(m_hWnd));
		return ::SendMessage(m_hWnd, SCI_GETENDATLASTLINE, (WPARAM)0, (LPARAM)0) != 0;
	}

	/// Retrieve the height of a particular line of text in pixels.
	int TextHeight(int line) {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_TEXTHEIGHT, (WPARAM)line, (LPARAM)0);
	}

	/// Show or hide the vertical scroll bar.
	void SetVScrollBar(bool visible) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SETVSCROLLBAR, (WPARAM)visible, (LPARAM)0);
	}

	/// Is the vertical scroll bar visible?
	bool GetVScrollBar() {
		ATLASSERT(::IsWindow(m_hWnd));
		return ::SendMessage(m_hWnd, SCI_GETVSCROLLBAR, (WPARAM)0, (LPARAM)0) != 0;
	}

	/// Append a string to the end of the document without changing the selection.
	void AppendText(int length, LPCSTR text) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_APPENDTEXT, (WPARAM)length, (LPARAM)text);
	}

	/// How many phases is drawing done in?
	int GetPhasesDraw() {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_GETPHASESDRAW, (WPARAM)0, (LPARAM)0);
	}

	/// In one phase draw, text is drawn in a series of rectangular blocks with no overlap.
	/// In two phase draw, text is drawn in a series of lines allowing runs to overlap horizontally.
	/// In multiple phase draw, each element is drawn over the whole drawing area, allowing text
	/// to overlap from one line to the next.
	void SetPhasesDraw(int phases) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SETPHASESDRAW, (WPARAM)phases, (LPARAM)0);
	}

	/// Choose the quality level for text from the FontQuality enumeration.
	void SetFontQuality(int fontQuality) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SETFONTQUALITY, (WPARAM)fontQuality, (LPARAM)0);
	}

	/// Retrieve the quality level for text.
	int GetFontQuality() {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_GETFONTQUALITY, (WPARAM)0, (LPARAM)0);
	}

	/// Scroll so that a display line is at the top of the display.
	void SetFirstVisibleLine(int displayLine) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SETFIRSTVISIBLELINE, (WPARAM)displayLine, (LPARAM)0);
	}

	/// Change the effect of pasting when there are multiple selections.
	void SetMultiPaste(int multiPaste) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SETMULTIPASTE, (WPARAM)multiPaste, (LPARAM)0);
	}

	/// Retrieve the effect of pasting when there are multiple selections.
	int GetMultiPaste() {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_GETMULTIPASTE, (WPARAM)0, (LPARAM)0);
	}

	/// Retrieve the value of a tag from a regular expression search.
	/// Result is NUL-terminated.
	int GetTag(int tagNumber, LPSTR tagValue) {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_GETTAG, (WPARAM)tagNumber, (LPARAM)tagValue);
	}

	/// Join the lines in the target.
	void LinesJoin() {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_LINESJOIN, (WPARAM)0, (LPARAM)0);
	}

	/// Split the lines in the target into lines that are less wide than pixelWidth
	/// where possible.
	void LinesSplit(int pixelWidth) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_LINESSPLIT, (WPARAM)pixelWidth, (LPARAM)0);
	}

	/// Set one of the colours used as a chequerboard pattern in the fold margin
	void SetFoldMarginColour(bool useSetting, COLORREF back) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SETFOLDMARGINCOLOUR, (WPARAM)useSetting, (LPARAM)back);
	}

	/// Set the other colour used as a chequerboard pattern in the fold margin
	void SetFoldMarginHiColour(bool useSetting, COLORREF fore) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SETFOLDMARGINHICOLOUR, (WPARAM)useSetting, (LPARAM)fore);
	}

	/// Enable or disable accessibility.
	void SetAccessibility(int accessibility) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SETACCESSIBILITY, (WPARAM)accessibility, (LPARAM)0);
	}

	/// Report accessibility status.
	int GetAccessibility() {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_GETACCESSIBILITY, (WPARAM)0, (LPARAM)0);
	}

	/// Move caret down one line.
	void LineDown() {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_LINEDOWN, (WPARAM)0, (LPARAM)0);
	}

	/// Move caret down one line extending selection to new caret position.
	void LineDownExtend() {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_LINEDOWNEXTEND, (WPARAM)0, (LPARAM)0);
	}

	/// Move caret up one line.
	void LineUp() {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_LINEUP, (WPARAM)0, (LPARAM)0);
	}

	/// Move caret up one line extending selection to new caret position.
	void LineUpExtend() {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_LINEUPEXTEND, (WPARAM)0, (LPARAM)0);
	}

	/// Move caret left one character.
	void CharLeft() {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_CHARLEFT, (WPARAM)0, (LPARAM)0);
	}

	/// Move caret left one character extending selection to new caret position.
	void CharLeftExtend() {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_CHARLEFTEXTEND, (WPARAM)0, (LPARAM)0);
	}

	/// Move caret right one character.
	void CharRight() {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_CHARRIGHT, (WPARAM)0, (LPARAM)0);
	}

	/// Move caret right one character extending selection to new caret position.
	void CharRightExtend() {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_CHARRIGHTEXTEND, (WPARAM)0, (LPARAM)0);
	}

	/// Move caret left one word.
	void WordLeft() {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_WORDLEFT, (WPARAM)0, (LPARAM)0);
	}

	/// Move caret left one word extending selection to new caret position.
	void WordLeftExtend() {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_WORDLEFTEXTEND, (WPARAM)0, (LPARAM)0);
	}

	/// Move caret right one word.
	void WordRight() {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_WORDRIGHT, (WPARAM)0, (LPARAM)0);
	}

	/// Move caret right one word extending selection to new caret position.
	void WordRightExtend() {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_WORDRIGHTEXTEND, (WPARAM)0, (LPARAM)0);
	}

	/// Move caret to first position on line.
	void Home() {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_HOME, (WPARAM)0, (LPARAM)0);
	}

	/// Move caret to first position on line extending selection to new caret position.
	void HomeExtend() {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_HOMEEXTEND, (WPARAM)0, (LPARAM)0);
	}

	/// Move caret to last position on line.
	void LineEnd() {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_LINEEND, (WPARAM)0, (LPARAM)0);
	}

	/// Move caret to last position on line extending selection to new caret position.
	void LineEndExtend() {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_LINEENDEXTEND, (WPARAM)0, (LPARAM)0);
	}

	/// Move caret to first position in document.
	void DocumentStart() {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_DOCUMENTSTART, (WPARAM)0, (LPARAM)0);
	}

	/// Move caret to first position in document extending selection to new caret position.
	void DocumentStartExtend() {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_DOCUMENTSTARTEXTEND, (WPARAM)0, (LPARAM)0);
	}

	/// Move caret to last position in document.
	void DocumentEnd() {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_DOCUMENTEND, (WPARAM)0, (LPARAM)0);
	}

	/// Move caret to last position in document extending selection to new caret position.
	void DocumentEndExtend() {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_DOCUMENTENDEXTEND, (WPARAM)0, (LPARAM)0);
	}

	/// Move caret one page up.
	void PageUp() {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_PAGEUP, (WPARAM)0, (LPARAM)0);
	}

	/// Move caret one page up extending selection to new caret position.
	void PageUpExtend() {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_PAGEUPEXTEND, (WPARAM)0, (LPARAM)0);
	}

	/// Move caret one page down.
	void PageDown() {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_PAGEDOWN, (WPARAM)0, (LPARAM)0);
	}

	/// Move caret one page down extending selection to new caret position.
	void PageDownExtend() {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_PAGEDOWNEXTEND, (WPARAM)0, (LPARAM)0);
	}

	/// Switch from insert to overtype mode or the reverse.
	void EditToggleOvertype() {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_EDITTOGGLEOVERTYPE, (WPARAM)0, (LPARAM)0);
	}

	/// Cancel any modes such as call tip or auto-completion list display.
	void Cancel() {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_CANCEL, (WPARAM)0, (LPARAM)0);
	}

	/// Delete the selection or if no selection, the character before the caret.
	void DeleteBack() {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_DELETEBACK, (WPARAM)0, (LPARAM)0);
	}

	/// If selection is empty or all on one line replace the selection with a tab character.
	/// If more than one line selected, indent the lines.
	void Tab() {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_TAB, (WPARAM)0, (LPARAM)0);
	}

	/// Indent the current and selected lines.
	void LineIndent() {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_LINEINDENT, (WPARAM)0, (LPARAM)0);
	}

	/// If selection is empty or all on one line dedent the line if caret is at start, else move caret.
	/// If more than one line selected, dedent the lines.
	void BackTab() {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_BACKTAB, (WPARAM)0, (LPARAM)0);
	}

	/// Dedent the current and selected lines.
	void LineDedent() {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_LINEDEDENT, (WPARAM)0, (LPARAM)0);
	}

	/// Insert a new line, may use a CRLF, CR or LF depending on EOL mode.
	void NewLine() {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_NEWLINE, (WPARAM)0, (LPARAM)0);
	}

	/// Insert a Form Feed character.
	void FormFeed() {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_FORMFEED, (WPARAM)0, (LPARAM)0);
	}

	/// Move caret to before first visible character on line.
	/// If already there move to first character on line.
	void VCHome() {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_VCHOME, (WPARAM)0, (LPARAM)0);
	}

	/// Like VCHome but extending selection to new caret position.
	void VCHomeExtend() {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_VCHOMEEXTEND, (WPARAM)0, (LPARAM)0);
	}

	/// Magnify the displayed text by increasing the sizes by 1 point.
	void ZoomIn() {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_ZOOMIN, (WPARAM)0, (LPARAM)0);
	}

	/// Make the displayed text smaller by decreasing the sizes by 1 point.
	void ZoomOut() {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_ZOOMOUT, (WPARAM)0, (LPARAM)0);
	}

	/// Delete the word to the left of the caret.
	void DelWordLeft() {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_DELWORDLEFT, (WPARAM)0, (LPARAM)0);
	}

	/// Delete the word to the right of the caret.
	void DelWordRight() {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_DELWORDRIGHT, (WPARAM)0, (LPARAM)0);
	}

	/// Delete the word to the right of the caret, but not the trailing non-word characters.
	void DelWordRightEnd() {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_DELWORDRIGHTEND, (WPARAM)0, (LPARAM)0);
	}

	/// Cut the line containing the caret.
	void LineCut() {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_LINECUT, (WPARAM)0, (LPARAM)0);
	}

	/// Delete the line containing the caret.
	void LineDelete() {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_LINEDELETE, (WPARAM)0, (LPARAM)0);
	}

	/// Switch the current line with the previous.
	void LineTranspose() {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_LINETRANSPOSE, (WPARAM)0, (LPARAM)0);
	}

	/// Reverse order of selected lines.
	void LineReverse() {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_LINEREVERSE, (WPARAM)0, (LPARAM)0);
	}

	/// Duplicate the current line.
	void LineDuplicate() {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_LINEDUPLICATE, (WPARAM)0, (LPARAM)0);
	}

	/// Transform the selection to lower case.
	void LowerCase() {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_LOWERCASE, (WPARAM)0, (LPARAM)0);
	}

	/// Transform the selection to upper case.
	void UpperCase() {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_UPPERCASE, (WPARAM)0, (LPARAM)0);
	}

	/// Scroll the document down, keeping the caret visible.
	void LineScrollDown() {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_LINESCROLLDOWN, (WPARAM)0, (LPARAM)0);
	}

	/// Scroll the document up, keeping the caret visible.
	void LineScrollUp() {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_LINESCROLLUP, (WPARAM)0, (LPARAM)0);
	}

	/// Delete the selection or if no selection, the character before the caret.
	/// Will not delete the character before at the start of a line.
	void DeleteBackNotLine() {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_DELETEBACKNOTLINE, (WPARAM)0, (LPARAM)0);
	}

	/// Move caret to first position on display line.
	void HomeDisplay() {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_HOMEDISPLAY, (WPARAM)0, (LPARAM)0);
	}

	/// Move caret to first position on display line extending selection to
	/// new caret position.
	void HomeDisplayExtend() {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_HOMEDISPLAYEXTEND, (WPARAM)0, (LPARAM)0);
	}

	/// Move caret to last position on display line.
	void LineEndDisplay() {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_LINEENDDISPLAY, (WPARAM)0, (LPARAM)0);
	}

	/// Move caret to last position on display line extending selection to new
	/// caret position.
	void LineEndDisplayExtend() {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_LINEENDDISPLAYEXTEND, (WPARAM)0, (LPARAM)0);
	}

	/// Like Home but when word-wrap is enabled goes first to start of display line
	/// HomeDisplay, then to start of document line Home.
	void HomeWrap() {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_HOMEWRAP, (WPARAM)0, (LPARAM)0);
	}

	/// Like HomeExtend but when word-wrap is enabled extends first to start of display line
	/// HomeDisplayExtend, then to start of document line HomeExtend.
	void HomeWrapExtend() {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_HOMEWRAPEXTEND, (WPARAM)0, (LPARAM)0);
	}

	/// Like LineEnd but when word-wrap is enabled goes first to end of display line
	/// LineEndDisplay, then to start of document line LineEnd.
	void LineEndWrap() {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_LINEENDWRAP, (WPARAM)0, (LPARAM)0);
	}

	/// Like LineEndExtend but when word-wrap is enabled extends first to end of display line
	/// LineEndDisplayExtend, then to start of document line LineEndExtend.
	void LineEndWrapExtend() {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_LINEENDWRAPEXTEND, (WPARAM)0, (LPARAM)0);
	}

	/// Like VCHome but when word-wrap is enabled goes first to start of display line
	/// VCHomeDisplay, then behaves like VCHome.
	void VCHomeWrap() {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_VCHOMEWRAP, (WPARAM)0, (LPARAM)0);
	}

	/// Like VCHomeExtend but when word-wrap is enabled extends first to start of display line
	/// VCHomeDisplayExtend, then behaves like VCHomeExtend.
	void VCHomeWrapExtend() {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_VCHOMEWRAPEXTEND, (WPARAM)0, (LPARAM)0);
	}

	/// Copy the line containing the caret.
	void LineCopy() {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_LINECOPY, (WPARAM)0, (LPARAM)0);
	}

	/// Move the caret inside current view if it's not there already.
	void MoveCaretInsideView() {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_MOVECARETINSIDEVIEW, (WPARAM)0, (LPARAM)0);
	}

	/// How many characters are on a line, including end of line characters?
	int LineLength(int line) {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_LINELENGTH, (WPARAM)line, (LPARAM)0);
	}

	/// Highlight the characters at two positions.
	void BraceHighlight(int posA, int posB) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_BRACEHIGHLIGHT, (WPARAM)posA, (LPARAM)posB);
	}

	/// Use specified indicator to highlight matching braces instead of changing their style.
	void BraceHighlightIndicator(bool useSetting, int indicator) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_BRACEHIGHLIGHTINDICATOR, (WPARAM)useSetting, (LPARAM)indicator);
	}

	/// Highlight the character at a position indicating there is no matching brace.
	void BraceBadLight(int pos) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_BRACEBADLIGHT, (WPARAM)pos, (LPARAM)0);
	}

	/// Use specified indicator to highlight non matching brace instead of changing its style.
	void BraceBadLightIndicator(bool useSetting, int indicator) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_BRACEBADLIGHTINDICATOR, (WPARAM)useSetting, (LPARAM)indicator);
	}

	/// Find the position of a matching brace or INVALID_POSITION if no match.
	/// The maxReStyle must be 0 for now. It may be defined in a future release.
	int BraceMatch(int pos, int maxReStyle) {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_BRACEMATCH, (WPARAM)pos, (LPARAM)maxReStyle);
	}

	/// Similar to BraceMatch, but matching starts at the explicit start position.
	int BraceMatchNext(int pos, int startPos) {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_BRACEMATCHNEXT, (WPARAM)pos, (LPARAM)startPos);
	}

	/// Are the end of line characters visible?
	bool GetViewEOL() {
		ATLASSERT(::IsWindow(m_hWnd));
		return ::SendMessage(m_hWnd, SCI_GETVIEWEOL, (WPARAM)0, (LPARAM)0) != 0;
	}

	/// Make the end of line characters visible or invisible.
	void SetViewEOL(bool visible) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SETVIEWEOL, (WPARAM)visible, (LPARAM)0);
	}

	/// Retrieve a pointer to the document object.
	void* GetDocPointer() {
		ATLASSERT(::IsWindow(m_hWnd));
		return (void*)::SendMessage(m_hWnd, SCI_GETDOCPOINTER, (WPARAM)0, (LPARAM)0);
	}

	/// Change the document object used.
	void SetDocPointer(void* doc) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SETDOCPOINTER, (WPARAM)0, (LPARAM)doc);
	}

	/// Set which document modification events are sent to the container.
	void SetModEventMask(int eventMask) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SETMODEVENTMASK, (WPARAM)eventMask, (LPARAM)0);
	}

	/// Retrieve the column number which text should be kept within.
	int GetEdgeColumn() {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_GETEDGECOLUMN, (WPARAM)0, (LPARAM)0);
	}

	/// Set the column number of the edge.
	/// If text goes past the edge then it is highlighted.
	void SetEdgeColumn(int column) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SETEDGECOLUMN, (WPARAM)column, (LPARAM)0);
	}

	/// Retrieve the edge highlight mode.
	int GetEdgeMode() {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_GETEDGEMODE, (WPARAM)0, (LPARAM)0);
	}

	/// The edge may be displayed by a line (EDGE_LINE/EDGE_MULTILINE) or by highlighting text that
	/// goes beyond it (EDGE_BACKGROUND) or not displayed at all (EDGE_NONE).
	void SetEdgeMode(int edgeMode) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SETEDGEMODE, (WPARAM)edgeMode, (LPARAM)0);
	}

	/// Retrieve the colour used in edge indication.
	COLORREF GetEdgeColour() {
		ATLASSERT(::IsWindow(m_hWnd));
		return (COLORREF)::SendMessage(m_hWnd, SCI_GETEDGECOLOUR, (WPARAM)0, (LPARAM)0);
	}

	/// Change the colour used in edge indication.
	void SetEdgeColour(COLORREF edgeColour) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SETEDGECOLOUR, (WPARAM)edgeColour, (LPARAM)0);
	}

	/// Add a new vertical edge to the view.
	void MultiEdgeAddLine(int column, COLORREF edgeColour) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_MULTIEDGEADDLINE, (WPARAM)column, (LPARAM)edgeColour);
	}

	/// Clear all vertical edges.
	void MultiEdgeClearAll() {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_MULTIEDGECLEARALL, (WPARAM)0, (LPARAM)0);
	}

	/// Get multi edge positions.
	int GetMultiEdgeColumn(int which) {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_GETMULTIEDGECOLUMN, (WPARAM)which, (LPARAM)0);
	}

	/// Sets the current caret position to be the search anchor.
	void SearchAnchor() {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SEARCHANCHOR, (WPARAM)0, (LPARAM)0);
	}

	/// Find some text starting at the search anchor.
	/// Does not ensure the selection is visible.
	int SearchNext(int searchFlags, LPCSTR text) {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_SEARCHNEXT, (WPARAM)searchFlags, (LPARAM)text);
	}

	/// Find some text starting at the search anchor and moving backwards.
	/// Does not ensure the selection is visible.
	int SearchPrev(int searchFlags, LPCSTR text) {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_SEARCHPREV, (WPARAM)searchFlags, (LPARAM)text);
	}

	/// Retrieves the number of lines completely visible.
	int LinesOnScreen() {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_LINESONSCREEN, (WPARAM)0, (LPARAM)0);
	}

	/// Set whether a pop up menu is displayed automatically when the user presses
	/// the wrong mouse button on certain areas.
	void UsePopUp(int popUpMode) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_USEPOPUP, (WPARAM)popUpMode, (LPARAM)0);
	}

	/// Is the selection rectangular? The alternative is the more common stream selection.
	bool SelectionIsRectangle() {
		ATLASSERT(::IsWindow(m_hWnd));
		return ::SendMessage(m_hWnd, SCI_SELECTIONISRECTANGLE, (WPARAM)0, (LPARAM)0) != 0;
	}

	/// Set the zoom level. This number of points is added to the size of all fonts.
	/// It may be positive to magnify or negative to reduce.
	void SetZoom(int zoomInPoints) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SETZOOM, (WPARAM)zoomInPoints, (LPARAM)0);
	}

	/// Retrieve the zoom level.
	int GetZoom() {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_GETZOOM, (WPARAM)0, (LPARAM)0);
	}

	/// Create a new document object.
	/// Starts with reference count of 1 and not selected into editor.
	void* CreateDocument(int bytes, int documentOptions) {
		ATLASSERT(::IsWindow(m_hWnd));
		return (void*)::SendMessage(m_hWnd, SCI_CREATEDOCUMENT, (WPARAM)bytes, (LPARAM)documentOptions);
	}

	/// Extend life of document.
	void AddRefDocument(void* doc) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_ADDREFDOCUMENT, (WPARAM)0, (LPARAM)doc);
	}

	/// Release a reference to the document, deleting document if it fades to black.
	void ReleaseDocument(void* doc) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_RELEASEDOCUMENT, (WPARAM)0, (LPARAM)doc);
	}

	/// Get which document options are set.
	int GetDocumentOptions() {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_GETDOCUMENTOPTIONS, (WPARAM)0, (LPARAM)0);
	}

	/// Get which document modification events are sent to the container.
	int GetModEventMask() {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_GETMODEVENTMASK, (WPARAM)0, (LPARAM)0);
	}

	/// Set whether command events are sent to the container.
	void SetCommandEvents(bool commandEvents) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SETCOMMANDEVENTS, (WPARAM)commandEvents, (LPARAM)0);
	}

	/// Get whether command events are sent to the container.
	bool GetCommandEvents() {
		ATLASSERT(::IsWindow(m_hWnd));
		return ::SendMessage(m_hWnd, SCI_GETCOMMANDEVENTS, (WPARAM)0, (LPARAM)0) != 0;
	}

	/// Change internal focus flag.
	void SetFocus(bool focus) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SETFOCUS, (WPARAM)focus, (LPARAM)0);
	}

	/// Get internal focus flag.
	bool GetFocus() {
		ATLASSERT(::IsWindow(m_hWnd));
		return ::SendMessage(m_hWnd, SCI_GETFOCUS, (WPARAM)0, (LPARAM)0) != 0;
	}

	/// Change error status - 0 = OK.
	void SetStatus(int status) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SETSTATUS, (WPARAM)status, (LPARAM)0);
	}

	/// Get error status.
	int GetStatus() {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_GETSTATUS, (WPARAM)0, (LPARAM)0);
	}

	/// Set whether the mouse is captured when its button is pressed.
	void SetMouseDownCaptures(bool captures) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SETMOUSEDOWNCAPTURES, (WPARAM)captures, (LPARAM)0);
	}

	/// Get whether mouse gets captured.
	bool GetMouseDownCaptures() {
		ATLASSERT(::IsWindow(m_hWnd));
		return ::SendMessage(m_hWnd, SCI_GETMOUSEDOWNCAPTURES, (WPARAM)0, (LPARAM)0) != 0;
	}

	/// Set whether the mouse wheel can be active outside the window.
	void SetMouseWheelCaptures(bool captures) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SETMOUSEWHEELCAPTURES, (WPARAM)captures, (LPARAM)0);
	}

	/// Get whether mouse wheel can be active outside the window.
	bool GetMouseWheelCaptures() {
		ATLASSERT(::IsWindow(m_hWnd));
		return ::SendMessage(m_hWnd, SCI_GETMOUSEWHEELCAPTURES, (WPARAM)0, (LPARAM)0) != 0;
	}

	/// Sets the cursor to one of the SC_CURSOR* values.
	void SetCursor(int cursorType) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SETCURSOR, (WPARAM)cursorType, (LPARAM)0);
	}

	/// Get cursor type.
	int GetCursor() {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_GETCURSOR, (WPARAM)0, (LPARAM)0);
	}

	/// Change the way control characters are displayed:
	/// If symbol is < 32, keep the drawn way, else, use the given character.
	void SetControlCharSymbol(int symbol) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SETCONTROLCHARSYMBOL, (WPARAM)symbol, (LPARAM)0);
	}

	/// Get the way control characters are displayed.
	int GetControlCharSymbol() {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_GETCONTROLCHARSYMBOL, (WPARAM)0, (LPARAM)0);
	}

	/// Move to the previous change in capitalisation.
	void WordPartLeft() {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_WORDPARTLEFT, (WPARAM)0, (LPARAM)0);
	}

	/// Move to the previous change in capitalisation extending selection
	/// to new caret position.
	void WordPartLeftExtend() {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_WORDPARTLEFTEXTEND, (WPARAM)0, (LPARAM)0);
	}

	/// Move to the change next in capitalisation.
	void WordPartRight() {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_WORDPARTRIGHT, (WPARAM)0, (LPARAM)0);
	}

	/// Move to the next change in capitalisation extending selection
	/// to new caret position.
	void WordPartRightExtend() {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_WORDPARTRIGHTEXTEND, (WPARAM)0, (LPARAM)0);
	}

	/// Set the way the display area is determined when a particular line
	/// is to be moved to by Find, FindNext, GotoLine, etc.
	void SetVisiblePolicy(int visiblePolicy, int visibleSlop) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SETVISIBLEPOLICY, (WPARAM)visiblePolicy, (LPARAM)visibleSlop);
	}

	/// Delete back from the current position to the start of the line.
	void DelLineLeft() {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_DELLINELEFT, (WPARAM)0, (LPARAM)0);
	}

	/// Delete forwards from the current position to the end of the line.
	void DelLineRight() {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_DELLINERIGHT, (WPARAM)0, (LPARAM)0);
	}

	/// Set the xOffset (ie, horizontal scroll position).
	void SetXOffset(int xOffset) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SETXOFFSET, (WPARAM)xOffset, (LPARAM)0);
	}

	/// Get the xOffset (ie, horizontal scroll position).
	int GetXOffset() {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_GETXOFFSET, (WPARAM)0, (LPARAM)0);
	}

	/// Set the last x chosen value to be the caret x position.
	void ChooseCaretX() {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_CHOOSECARETX, (WPARAM)0, (LPARAM)0);
	}

	/// Set the focus to this Scintilla widget.
	void GrabFocus() {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_GRABFOCUS, (WPARAM)0, (LPARAM)0);
	}

	/// Set the way the caret is kept visible when going sideways.
	/// The exclusion zone is given in pixels.
	void SetXCaretPolicy(int caretPolicy, int caretSlop) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SETXCARETPOLICY, (WPARAM)caretPolicy, (LPARAM)caretSlop);
	}

	/// Set the way the line the caret is on is kept visible.
	/// The exclusion zone is given in lines.
	void SetYCaretPolicy(int caretPolicy, int caretSlop) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SETYCARETPOLICY, (WPARAM)caretPolicy, (LPARAM)caretSlop);
	}

	/// Set printing to line wrapped (SC_WRAP_WORD) or not line wrapped (SC_WRAP_NONE).
	void SetPrintWrapMode(int wrapMode) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SETPRINTWRAPMODE, (WPARAM)wrapMode, (LPARAM)0);
	}

	/// Is printing line wrapped?
	int GetPrintWrapMode() {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_GETPRINTWRAPMODE, (WPARAM)0, (LPARAM)0);
	}

	/// Set a fore colour for active hotspots.
	void SetHotspotActiveFore(bool useSetting, COLORREF fore) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SETHOTSPOTACTIVEFORE, (WPARAM)useSetting, (LPARAM)fore);
	}

	/// Get the fore colour for active hotspots.
	COLORREF GetHotspotActiveFore() {
		ATLASSERT(::IsWindow(m_hWnd));
		return (COLORREF)::SendMessage(m_hWnd, SCI_GETHOTSPOTACTIVEFORE, (WPARAM)0, (LPARAM)0);
	}

	/// Set a back colour for active hotspots.
	void SetHotspotActiveBack(bool useSetting, COLORREF back) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SETHOTSPOTACTIVEBACK, (WPARAM)useSetting, (LPARAM)back);
	}

	/// Get the back colour for active hotspots.
	COLORREF GetHotspotActiveBack() {
		ATLASSERT(::IsWindow(m_hWnd));
		return (COLORREF)::SendMessage(m_hWnd, SCI_GETHOTSPOTACTIVEBACK, (WPARAM)0, (LPARAM)0);
	}

	/// Enable / Disable underlining active hotspots.
	void SetHotspotActiveUnderline(bool underline) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SETHOTSPOTACTIVEUNDERLINE, (WPARAM)underline, (LPARAM)0);
	}

	/// Get whether underlining for active hotspots.
	bool GetHotspotActiveUnderline() {
		ATLASSERT(::IsWindow(m_hWnd));
		return ::SendMessage(m_hWnd, SCI_GETHOTSPOTACTIVEUNDERLINE, (WPARAM)0, (LPARAM)0) != 0;
	}

	/// Limit hotspots to single line so hotspots on two lines don't merge.
	void SetHotspotSingleLine(bool singleLine) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SETHOTSPOTSINGLELINE, (WPARAM)singleLine, (LPARAM)0);
	}

	/// Get the HotspotSingleLine property
	bool GetHotspotSingleLine() {
		ATLASSERT(::IsWindow(m_hWnd));
		return ::SendMessage(m_hWnd, SCI_GETHOTSPOTSINGLELINE, (WPARAM)0, (LPARAM)0) != 0;
	}

	/// Move caret down one paragraph (delimited by empty lines).
	void ParaDown() {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_PARADOWN, (WPARAM)0, (LPARAM)0);
	}

	/// Extend selection down one paragraph (delimited by empty lines).
	void ParaDownExtend() {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_PARADOWNEXTEND, (WPARAM)0, (LPARAM)0);
	}

	/// Move caret up one paragraph (delimited by empty lines).
	void ParaUp() {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_PARAUP, (WPARAM)0, (LPARAM)0);
	}

	/// Extend selection up one paragraph (delimited by empty lines).
	void ParaUpExtend() {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_PARAUPEXTEND, (WPARAM)0, (LPARAM)0);
	}

	/// Given a valid document position, return the previous position taking code
	/// page into account. Returns 0 if passed 0.
	int PositionBefore(int pos) {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_POSITIONBEFORE, (WPARAM)pos, (LPARAM)0);
	}

	/// Given a valid document position, return the next position taking code
	/// page into account. Maximum value returned is the last position in the document.
	int PositionAfter(int pos) {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_POSITIONAFTER, (WPARAM)pos, (LPARAM)0);
	}

	/// Given a valid document position, return a position that differs in a number
	/// of characters. Returned value is always between 0 and last position in document.
	int PositionRelative(int pos, int relative) {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_POSITIONRELATIVE, (WPARAM)pos, (LPARAM)relative);
	}

	/// Given a valid document position, return a position that differs in a number
	/// of UTF-16 code units. Returned value is always between 0 and last position in document.
	/// The result may point half way (2 bytes) inside a non-BMP character.
	int PositionRelativeCodeUnits(int pos, int relative) {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_POSITIONRELATIVECODEUNITS, (WPARAM)pos, (LPARAM)relative);
	}

	/// Copy a range of text to the clipboard. Positions are clipped into the document.
	void CopyRange(int start, int end) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_COPYRANGE, (WPARAM)start, (LPARAM)end);
	}

	/// Copy argument text to the clipboard.
	void CopyText(int length, LPCSTR text) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_COPYTEXT, (WPARAM)length, (LPARAM)text);
	}

	/// Set the selection mode to stream (SC_SEL_STREAM) or rectangular (SC_SEL_RECTANGLE/SC_SEL_THIN) or
	/// by lines (SC_SEL_LINES).
	void SetSelectionMode(int selectionMode) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SETSELECTIONMODE, (WPARAM)selectionMode, (LPARAM)0);
	}

	/// Set the selection mode to stream (SC_SEL_STREAM) or rectangular (SC_SEL_RECTANGLE/SC_SEL_THIN) or
	/// by lines (SC_SEL_LINES) without changing MoveExtendsSelection.
	void ChangeSelectionMode(int selectionMode) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_CHANGESELECTIONMODE, (WPARAM)selectionMode, (LPARAM)0);
	}

	/// Get the mode of the current selection.
	int GetSelectionMode() {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_GETSELECTIONMODE, (WPARAM)0, (LPARAM)0);
	}

	/// Set whether or not regular caret moves will extend or reduce the selection.
	void SetMoveExtendsSelection(bool moveExtendsSelection) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SETMOVEEXTENDSSELECTION, (WPARAM)moveExtendsSelection, (LPARAM)0);
	}

	/// Get whether or not regular caret moves will extend or reduce the selection.
	bool GetMoveExtendsSelection() {
		ATLASSERT(::IsWindow(m_hWnd));
		return ::SendMessage(m_hWnd, SCI_GETMOVEEXTENDSSELECTION, (WPARAM)0, (LPARAM)0) != 0;
	}

	/// Retrieve the position of the start of the selection at the given line (INVALID_POSITION if no selection on this line).
	int GetLineSelStartPosition(int line) {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_GETLINESELSTARTPOSITION, (WPARAM)line, (LPARAM)0);
	}

	/// Retrieve the position of the end of the selection at the given line (INVALID_POSITION if no selection on this line).
	int GetLineSelEndPosition(int line) {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_GETLINESELENDPOSITION, (WPARAM)line, (LPARAM)0);
	}

	/// Move caret down one line, extending rectangular selection to new caret position.
	void LineDownRectExtend() {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_LINEDOWNRECTEXTEND, (WPARAM)0, (LPARAM)0);
	}

	/// Move caret up one line, extending rectangular selection to new caret position.
	void LineUpRectExtend() {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_LINEUPRECTEXTEND, (WPARAM)0, (LPARAM)0);
	}

	/// Move caret left one character, extending rectangular selection to new caret position.
	void CharLeftRectExtend() {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_CHARLEFTRECTEXTEND, (WPARAM)0, (LPARAM)0);
	}

	/// Move caret right one character, extending rectangular selection to new caret position.
	void CharRightRectExtend() {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_CHARRIGHTRECTEXTEND, (WPARAM)0, (LPARAM)0);
	}

	/// Move caret to first position on line, extending rectangular selection to new caret position.
	void HomeRectExtend() {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_HOMERECTEXTEND, (WPARAM)0, (LPARAM)0);
	}

	/// Move caret to before first visible character on line.
	/// If already there move to first character on line.
	/// In either case, extend rectangular selection to new caret position.
	void VCHomeRectExtend() {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_VCHOMERECTEXTEND, (WPARAM)0, (LPARAM)0);
	}

	/// Move caret to last position on line, extending rectangular selection to new caret position.
	void LineEndRectExtend() {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_LINEENDRECTEXTEND, (WPARAM)0, (LPARAM)0);
	}

	/// Move caret one page up, extending rectangular selection to new caret position.
	void PageUpRectExtend() {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_PAGEUPRECTEXTEND, (WPARAM)0, (LPARAM)0);
	}

	/// Move caret one page down, extending rectangular selection to new caret position.
	void PageDownRectExtend() {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_PAGEDOWNRECTEXTEND, (WPARAM)0, (LPARAM)0);
	}

	/// Move caret to top of page, or one page up if already at top of page.
	void StutteredPageUp() {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_STUTTEREDPAGEUP, (WPARAM)0, (LPARAM)0);
	}

	/// Move caret to top of page, or one page up if already at top of page, extending selection to new caret position.
	void StutteredPageUpExtend() {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_STUTTEREDPAGEUPEXTEND, (WPARAM)0, (LPARAM)0);
	}

	/// Move caret to bottom of page, or one page down if already at bottom of page.
	void StutteredPageDown() {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_STUTTEREDPAGEDOWN, (WPARAM)0, (LPARAM)0);
	}

	/// Move caret to bottom of page, or one page down if already at bottom of page, extending selection to new caret position.
	void StutteredPageDownExtend() {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_STUTTEREDPAGEDOWNEXTEND, (WPARAM)0, (LPARAM)0);
	}

	/// Move caret left one word, position cursor at end of word.
	void WordLeftEnd() {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_WORDLEFTEND, (WPARAM)0, (LPARAM)0);
	}

	/// Move caret left one word, position cursor at end of word, extending selection to new caret position.
	void WordLeftEndExtend() {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_WORDLEFTENDEXTEND, (WPARAM)0, (LPARAM)0);
	}

	/// Move caret right one word, position cursor at end of word.
	void WordRightEnd() {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_WORDRIGHTEND, (WPARAM)0, (LPARAM)0);
	}

	/// Move caret right one word, position cursor at end of word, extending selection to new caret position.
	void WordRightEndExtend() {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_WORDRIGHTENDEXTEND, (WPARAM)0, (LPARAM)0);
	}

	/// Set the set of characters making up whitespace for when moving or selecting by word.
	/// Should be called after SetWordChars.
	void SetWhitespaceChars(LPCSTR characters) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SETWHITESPACECHARS, (WPARAM)0, (LPARAM)characters);
	}

	/// Get the set of characters making up whitespace for when moving or selecting by word.
	int GetWhitespaceChars(LPSTR characters) {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_GETWHITESPACECHARS, (WPARAM)0, (LPARAM)characters);
	}

	/// Set the set of characters making up punctuation characters
	/// Should be called after SetWordChars.
	void SetPunctuationChars(LPCSTR characters) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SETPUNCTUATIONCHARS, (WPARAM)0, (LPARAM)characters);
	}

	/// Get the set of characters making up punctuation characters
	int GetPunctuationChars(LPSTR characters) {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_GETPUNCTUATIONCHARS, (WPARAM)0, (LPARAM)characters);
	}

	/// Reset the set of characters for whitespace and word characters to the defaults.
	void SetCharsDefault() {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SETCHARSDEFAULT, (WPARAM)0, (LPARAM)0);
	}

	/// Get currently selected item position in the auto-completion list
	int AutoCGetCurrent() {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_AUTOCGETCURRENT, (WPARAM)0, (LPARAM)0);
	}

	/// Get currently selected item text in the auto-completion list
	/// Returns the length of the item text
	/// Result is NUL-terminated.
	int AutoCGetCurrentText(LPSTR text) {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_AUTOCGETCURRENTTEXT, (WPARAM)0, (LPARAM)text);
	}

	/// Set auto-completion case insensitive behaviour to either prefer case-sensitive matches or have no preference.
	void AutoCSetCaseInsensitiveBehaviour(int behaviour) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_AUTOCSETCASEINSENSITIVEBEHAVIOUR, (WPARAM)behaviour, (LPARAM)0);
	}

	/// Get auto-completion case insensitive behaviour.
	int AutoCGetCaseInsensitiveBehaviour() {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_AUTOCGETCASEINSENSITIVEBEHAVIOUR, (WPARAM)0, (LPARAM)0);
	}

	/// Change the effect of autocompleting when there are multiple selections.
	void AutoCSetMulti(int multi) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_AUTOCSETMULTI, (WPARAM)multi, (LPARAM)0);
	}

	/// Retrieve the effect of autocompleting when there are multiple selections.
	int AutoCGetMulti() {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_AUTOCGETMULTI, (WPARAM)0, (LPARAM)0);
	}

	/// Set the way autocompletion lists are ordered.
	void AutoCSetOrder(int order) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_AUTOCSETORDER, (WPARAM)order, (LPARAM)0);
	}

	/// Get the way autocompletion lists are ordered.
	int AutoCGetOrder() {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_AUTOCGETORDER, (WPARAM)0, (LPARAM)0);
	}

	/// Enlarge the document to a particular size of text bytes.
	void Allocate(int bytes) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_ALLOCATE, (WPARAM)bytes, (LPARAM)0);
	}

	/// Returns the target converted to UTF8.
	/// Return the length in bytes.
	int TargetAsUTF8(LPSTR s) {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_TARGETASUTF8, (WPARAM)0, (LPARAM)s);
	}

	/// Set the length of the utf8 argument for calling EncodedFromUTF8.
	/// Set to -1 and the string will be measured to the first nul.
	void SetLengthForEncode(int bytes) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SETLENGTHFORENCODE, (WPARAM)bytes, (LPARAM)0);
	}

	/// Translates a UTF8 string into the document encoding.
	/// Return the length of the result in bytes.
	/// On error return 0.
	int EncodedFromUTF8(LPCSTR utf8, LPSTR encoded) {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_ENCODEDFROMUTF8, (WPARAM)utf8, (LPARAM)encoded);
	}

	/// Find the position of a column on a line taking into account tabs and
	/// multi-byte characters. If beyond end of line, return line end position.
	int FindColumn(int line, int column) {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_FINDCOLUMN, (WPARAM)line, (LPARAM)column);
	}

	/// Can the caret preferred x position only be changed by explicit movement commands?
	int GetCaretSticky() {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_GETCARETSTICKY, (WPARAM)0, (LPARAM)0);
	}

	/// Stop the caret preferred x position changing when the user types.
	void SetCaretSticky(int useCaretStickyBehaviour) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SETCARETSTICKY, (WPARAM)useCaretStickyBehaviour, (LPARAM)0);
	}

	/// Switch between sticky and non-sticky: meant to be bound to a key.
	void ToggleCaretSticky() {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_TOGGLECARETSTICKY, (WPARAM)0, (LPARAM)0);
	}

	/// Enable/Disable convert-on-paste for line endings
	void SetPasteConvertEndings(bool convert) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SETPASTECONVERTENDINGS, (WPARAM)convert, (LPARAM)0);
	}

	/// Get convert-on-paste setting
	bool GetPasteConvertEndings() {
		ATLASSERT(::IsWindow(m_hWnd));
		return ::SendMessage(m_hWnd, SCI_GETPASTECONVERTENDINGS, (WPARAM)0, (LPARAM)0) != 0;
	}

	/// Replace the selection with text like a rectangular paste.
	void ReplaceRectangular(int length, LPCSTR text) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_REPLACERECTANGULAR, (WPARAM)length, (LPARAM)text);
	}

	/// Duplicate the selection. If selection empty duplicate the line containing the caret.
	void SelectionDuplicate() {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SELECTIONDUPLICATE, (WPARAM)0, (LPARAM)0);
	}

	/// Set background alpha of the caret line.
	void SetCaretLineBackAlpha(int alpha) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SETCARETLINEBACKALPHA, (WPARAM)alpha, (LPARAM)0);
	}

	/// Get the background alpha of the caret line.
	int GetCaretLineBackAlpha() {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_GETCARETLINEBACKALPHA, (WPARAM)0, (LPARAM)0);
	}

	/// Set the style of the caret to be drawn.
	void SetCaretStyle(int caretStyle) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SETCARETSTYLE, (WPARAM)caretStyle, (LPARAM)0);
	}

	/// Returns the current style of the caret.
	int GetCaretStyle() {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_GETCARETSTYLE, (WPARAM)0, (LPARAM)0);
	}

	/// Set the indicator used for IndicatorFillRange and IndicatorClearRange
	void SetIndicatorCurrent(int indicator) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SETINDICATORCURRENT, (WPARAM)indicator, (LPARAM)0);
	}

	/// Get the current indicator
	int GetIndicatorCurrent() {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_GETINDICATORCURRENT, (WPARAM)0, (LPARAM)0);
	}

	/// Set the value used for IndicatorFillRange
	void SetIndicatorValue(int value) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SETINDICATORVALUE, (WPARAM)value, (LPARAM)0);
	}

	/// Get the current indicator value
	int GetIndicatorValue() {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_GETINDICATORVALUE, (WPARAM)0, (LPARAM)0);
	}

	/// Turn a indicator on over a range.
	void IndicatorFillRange(int start, int lengthFill) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_INDICATORFILLRANGE, (WPARAM)start, (LPARAM)lengthFill);
	}

	/// Turn a indicator off over a range.
	void IndicatorClearRange(int start, int lengthClear) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_INDICATORCLEARRANGE, (WPARAM)start, (LPARAM)lengthClear);
	}

	/// Are any indicators present at pos?
	int IndicatorAllOnFor(int pos) {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_INDICATORALLONFOR, (WPARAM)pos, (LPARAM)0);
	}

	/// What value does a particular indicator have at a position?
	int IndicatorValueAt(int indicator, int pos) {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_INDICATORVALUEAT, (WPARAM)indicator, (LPARAM)pos);
	}

	/// Where does a particular indicator start?
	int IndicatorStart(int indicator, int pos) {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_INDICATORSTART, (WPARAM)indicator, (LPARAM)pos);
	}

	/// Where does a particular indicator end?
	int IndicatorEnd(int indicator, int pos) {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_INDICATOREND, (WPARAM)indicator, (LPARAM)pos);
	}

	/// Set number of entries in position cache
	void SetPositionCache(int size) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SETPOSITIONCACHE, (WPARAM)size, (LPARAM)0);
	}

	/// How many entries are allocated to the position cache?
	int GetPositionCache() {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_GETPOSITIONCACHE, (WPARAM)0, (LPARAM)0);
	}

	/// Set maximum number of threads used for layout
	void SetLayoutThreads(int threads) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SETLAYOUTTHREADS, (WPARAM)threads, (LPARAM)0);
	}

	/// Get maximum number of threads used for layout
	int GetLayoutThreads() {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_GETLAYOUTTHREADS, (WPARAM)0, (LPARAM)0);
	}

	/// Copy the selection, if selection empty copy the line with the caret
	void CopyAllowLine() {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_COPYALLOWLINE, (WPARAM)0, (LPARAM)0);
	}

	/// Cut the selection, if selection empty cut the line with the caret
	void CutAllowLine() {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_CUTALLOWLINE, (WPARAM)0, (LPARAM)0);
	}

	/// Set the string to separate parts when copying a multiple selection.
	void SetCopySeparator(LPCSTR separator) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SETCOPYSEPARATOR, (WPARAM)0, (LPARAM)separator);
	}

	/// Get the string to separate parts when copying a multiple selection.
	int GetCopySeparator(LPSTR separator) {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_GETCOPYSEPARATOR, (WPARAM)0, (LPARAM)separator);
	}

	/// Compact the document buffer and return a read-only pointer to the
	/// characters in the document.
	void* GetCharacterPointer() {
		ATLASSERT(::IsWindow(m_hWnd));
		return (void*)::SendMessage(m_hWnd, SCI_GETCHARACTERPOINTER, (WPARAM)0, (LPARAM)0);
	}

	/// Return a read-only pointer to a range of characters in the document.
	/// May move the gap so that the range is contiguous, but will only move up
	/// to lengthRange bytes.
	void* GetRangePointer(int start, int lengthRange) {
		ATLASSERT(::IsWindow(m_hWnd));
		return (void*)::SendMessage(m_hWnd, SCI_GETRANGEPOINTER, (WPARAM)start, (LPARAM)lengthRange);
	}

	/// Return a position which, to avoid performance costs, should not be within
	/// the range of a call to GetRangePointer.
	int GetGapPosition() {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_GETGAPPOSITION, (WPARAM)0, (LPARAM)0);
	}

	/// Set the alpha fill colour of the given indicator.
	void IndicSetAlpha(int indicator, int alpha) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_INDICSETALPHA, (WPARAM)indicator, (LPARAM)alpha);
	}

	/// Get the alpha fill colour of the given indicator.
	int IndicGetAlpha(int indicator) {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_INDICGETALPHA, (WPARAM)indicator, (LPARAM)0);
	}

	/// Set the alpha outline colour of the given indicator.
	void IndicSetOutlineAlpha(int indicator, int alpha) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_INDICSETOUTLINEALPHA, (WPARAM)indicator, (LPARAM)alpha);
	}

	/// Get the alpha outline colour of the given indicator.
	int IndicGetOutlineAlpha(int indicator) {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_INDICGETOUTLINEALPHA, (WPARAM)indicator, (LPARAM)0);
	}

	/// Set extra ascent for each line
	void SetExtraAscent(int extraAscent) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SETEXTRAASCENT, (WPARAM)extraAscent, (LPARAM)0);
	}

	/// Get extra ascent for each line
	int GetExtraAscent() {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_GETEXTRAASCENT, (WPARAM)0, (LPARAM)0);
	}

	/// Set extra descent for each line
	void SetExtraDescent(int extraDescent) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SETEXTRADESCENT, (WPARAM)extraDescent, (LPARAM)0);
	}

	/// Get extra descent for each line
	int GetExtraDescent() {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_GETEXTRADESCENT, (WPARAM)0, (LPARAM)0);
	}

	/// Which symbol was defined for markerNumber with MarkerDefine
	int MarkerSymbolDefined(int markerNumber) {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_MARKERSYMBOLDEFINED, (WPARAM)markerNumber, (LPARAM)0);
	}

	/// Set the text in the text margin for a line
	void MarginSetText(int line, LPCSTR text) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_MARGINSETTEXT, (WPARAM)line, (LPARAM)text);
	}

	/// Get the text in the text margin for a line
	int MarginGetText(int line, LPSTR text) {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_MARGINGETTEXT, (WPARAM)line, (LPARAM)text);
	}

	/// Set the style number for the text margin for a line
	void MarginSetStyle(int line, int style) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_MARGINSETSTYLE, (WPARAM)line, (LPARAM)style);
	}

	/// Get the style number for the text margin for a line
	int MarginGetStyle(int line) {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_MARGINGETSTYLE, (WPARAM)line, (LPARAM)0);
	}

	/// Set the style in the text margin for a line
	void MarginSetStyles(int line, LPCSTR styles) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_MARGINSETSTYLES, (WPARAM)line, (LPARAM)styles);
	}

	/// Get the styles in the text margin for a line
	int MarginGetStyles(int line, LPSTR styles) {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_MARGINGETSTYLES, (WPARAM)line, (LPARAM)styles);
	}

	/// Clear the margin text on all lines
	void MarginTextClearAll() {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_MARGINTEXTCLEARALL, (WPARAM)0, (LPARAM)0);
	}

	/// Set the start of the range of style numbers used for margin text
	void MarginSetStyleOffset(int style) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_MARGINSETSTYLEOFFSET, (WPARAM)style, (LPARAM)0);
	}

	/// Get the start of the range of style numbers used for margin text
	int MarginGetStyleOffset() {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_MARGINGETSTYLEOFFSET, (WPARAM)0, (LPARAM)0);
	}

	/// Set the margin options.
	void SetMarginOptions(int marginOptions) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SETMARGINOPTIONS, (WPARAM)marginOptions, (LPARAM)0);
	}

	/// Get the margin options.
	int GetMarginOptions() {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_GETMARGINOPTIONS, (WPARAM)0, (LPARAM)0);
	}

	/// Set the annotation text for a line
	void AnnotationSetText(int line, LPCSTR text) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_ANNOTATIONSETTEXT, (WPARAM)line, (LPARAM)text);
	}

	/// Get the annotation text for a line
	int AnnotationGetText(int line, LPSTR text) {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_ANNOTATIONGETTEXT, (WPARAM)line, (LPARAM)text);
	}

	/// Set the style number for the annotations for a line
	void AnnotationSetStyle(int line, int style) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_ANNOTATIONSETSTYLE, (WPARAM)line, (LPARAM)style);
	}

	/// Get the style number for the annotations for a line
	int AnnotationGetStyle(int line) {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_ANNOTATIONGETSTYLE, (WPARAM)line, (LPARAM)0);
	}

	/// Set the annotation styles for a line
	void AnnotationSetStyles(int line, LPCSTR styles) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_ANNOTATIONSETSTYLES, (WPARAM)line, (LPARAM)styles);
	}

	/// Get the annotation styles for a line
	int AnnotationGetStyles(int line, LPSTR styles) {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_ANNOTATIONGETSTYLES, (WPARAM)line, (LPARAM)styles);
	}

	/// Get the number of annotation lines for a line
	int AnnotationGetLines(int line) {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_ANNOTATIONGETLINES, (WPARAM)line, (LPARAM)0);
	}

	/// Clear the annotations from all lines
	void AnnotationClearAll() {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_ANNOTATIONCLEARALL, (WPARAM)0, (LPARAM)0);
	}

	/// Set the visibility for the annotations for a view
	void AnnotationSetVisible(int visible) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_ANNOTATIONSETVISIBLE, (WPARAM)visible, (LPARAM)0);
	}

	/// Get the visibility for the annotations for a view
	int AnnotationGetVisible() {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_ANNOTATIONGETVISIBLE, (WPARAM)0, (LPARAM)0);
	}

	/// Set the start of the range of style numbers used for annotations
	void AnnotationSetStyleOffset(int style) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_ANNOTATIONSETSTYLEOFFSET, (WPARAM)style, (LPARAM)0);
	}

	/// Get the start of the range of style numbers used for annotations
	int AnnotationGetStyleOffset() {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_ANNOTATIONGETSTYLEOFFSET, (WPARAM)0, (LPARAM)0);
	}

	/// Release all extended (>255) style numbers
	void ReleaseAllExtendedStyles() {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_RELEASEALLEXTENDEDSTYLES, (WPARAM)0, (LPARAM)0);
	}

	/// Allocate some extended (>255) style numbers and return the start of the range
	int AllocateExtendedStyles(int numberStyles) {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_ALLOCATEEXTENDEDSTYLES, (WPARAM)numberStyles, (LPARAM)0);
	}

	/// Add a container action to the undo stack
	void AddUndoAction(int token, int flags) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_ADDUNDOACTION, (WPARAM)token, (LPARAM)flags);
	}

	/// Find the position of a character from a point within the window.
	int CharPositionFromPoint(int x, int y) {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_CHARPOSITIONFROMPOINT, (WPARAM)x, (LPARAM)y);
	}

	/// Find the position of a character from a point within the window.
	/// Return INVALID_POSITION if not close to text.
	int CharPositionFromPointClose(int x, int y) {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_CHARPOSITIONFROMPOINTCLOSE, (WPARAM)x, (LPARAM)y);
	}

	/// Set whether switching to rectangular mode while selecting with the mouse is allowed.
	void SetMouseSelectionRectangularSwitch(bool mouseSelectionRectangularSwitch) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SETMOUSESELECTIONRECTANGULARSWITCH, (WPARAM)mouseSelectionRectangularSwitch, (LPARAM)0);
	}

	/// Whether switching to rectangular mode while selecting with the mouse is allowed.
	bool GetMouseSelectionRectangularSwitch() {
		ATLASSERT(::IsWindow(m_hWnd));
		return ::SendMessage(m_hWnd, SCI_GETMOUSESELECTIONRECTANGULARSWITCH, (WPARAM)0, (LPARAM)0) != 0;
	}

	/// Set mouse mapping.
	void SetMouseMapping(int mouseMapping) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SETMOUSEMAPPING, (WPARAM)mouseMapping, (LPARAM)0);
	}

	/// Get mouse mapping.
	int GetMouseMapping() {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_GETMOUSEMAPPING, (WPARAM)0, (LPARAM)0);
	}

	/// Set whether multiple selections can be made
	void SetMultipleSelection(bool multipleSelection) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SETMULTIPLESELECTION, (WPARAM)multipleSelection, (LPARAM)0);
	}

	/// Whether multiple selections can be made
	bool GetMultipleSelection() {
		ATLASSERT(::IsWindow(m_hWnd));
		return ::SendMessage(m_hWnd, SCI_GETMULTIPLESELECTION, (WPARAM)0, (LPARAM)0) != 0;
	}

	/// Set whether typing can be performed into multiple selections
	void SetAdditionalSelectionTyping(bool additionalSelectionTyping) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SETADDITIONALSELECTIONTYPING, (WPARAM)additionalSelectionTyping, (LPARAM)0);
	}

	/// Whether typing can be performed into multiple selections
	bool GetAdditionalSelectionTyping() {
		ATLASSERT(::IsWindow(m_hWnd));
		return ::SendMessage(m_hWnd, SCI_GETADDITIONALSELECTIONTYPING, (WPARAM)0, (LPARAM)0) != 0;
	}

	/// Set whether additional carets will blink
	void SetAdditionalCaretsBlink(bool additionalCaretsBlink) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SETADDITIONALCARETSBLINK, (WPARAM)additionalCaretsBlink, (LPARAM)0);
	}

	/// Whether additional carets will blink
	bool GetAdditionalCaretsBlink() {
		ATLASSERT(::IsWindow(m_hWnd));
		return ::SendMessage(m_hWnd, SCI_GETADDITIONALCARETSBLINK, (WPARAM)0, (LPARAM)0) != 0;
	}

	/// Set whether additional carets are visible
	void SetAdditionalCaretsVisible(bool additionalCaretsVisible) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SETADDITIONALCARETSVISIBLE, (WPARAM)additionalCaretsVisible, (LPARAM)0);
	}

	/// Whether additional carets are visible
	bool GetAdditionalCaretsVisible() {
		ATLASSERT(::IsWindow(m_hWnd));
		return ::SendMessage(m_hWnd, SCI_GETADDITIONALCARETSVISIBLE, (WPARAM)0, (LPARAM)0) != 0;
	}

	/// How many selections are there?
	int GetSelections() {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_GETSELECTIONS, (WPARAM)0, (LPARAM)0);
	}

	/// Is every selected range empty?
	bool GetSelectionEmpty() {
		ATLASSERT(::IsWindow(m_hWnd));
		return ::SendMessage(m_hWnd, SCI_GETSELECTIONEMPTY, (WPARAM)0, (LPARAM)0) != 0;
	}

	/// Clear selections to a single empty stream selection
	void ClearSelections() {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_CLEARSELECTIONS, (WPARAM)0, (LPARAM)0);
	}

	/// Set a simple selection
	void SetSelection(int caret, int anchor) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SETSELECTION, (WPARAM)caret, (LPARAM)anchor);
	}

	/// Add a selection
	void AddSelection(int caret, int anchor) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_ADDSELECTION, (WPARAM)caret, (LPARAM)anchor);
	}

	/// Find the selection index for a point. -1 when not at a selection.
	int SelectionFromPoint(int x, int y) {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_SELECTIONFROMPOINT, (WPARAM)x, (LPARAM)y);
	}

	/// Drop one selection
	void DropSelectionN(int selection) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_DROPSELECTIONN, (WPARAM)selection, (LPARAM)0);
	}

	/// Set the main selection
	void SetMainSelection(int selection) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SETMAINSELECTION, (WPARAM)selection, (LPARAM)0);
	}

	/// Which selection is the main selection
	int GetMainSelection() {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_GETMAINSELECTION, (WPARAM)0, (LPARAM)0);
	}

	/// Set the caret position of the nth selection.
	void SetSelectionNCaret(int selection, int caret) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SETSELECTIONNCARET, (WPARAM)selection, (LPARAM)caret);
	}

	/// Return the caret position of the nth selection.
	int GetSelectionNCaret(int selection) {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_GETSELECTIONNCARET, (WPARAM)selection, (LPARAM)0);
	}

	/// Set the anchor position of the nth selection.
	void SetSelectionNAnchor(int selection, int anchor) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SETSELECTIONNANCHOR, (WPARAM)selection, (LPARAM)anchor);
	}

	/// Return the anchor position of the nth selection.
	int GetSelectionNAnchor(int selection) {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_GETSELECTIONNANCHOR, (WPARAM)selection, (LPARAM)0);
	}

	/// Set the virtual space of the caret of the nth selection.
	void SetSelectionNCaretVirtualSpace(int selection, int space) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SETSELECTIONNCARETVIRTUALSPACE, (WPARAM)selection, (LPARAM)space);
	}

	/// Return the virtual space of the caret of the nth selection.
	int GetSelectionNCaretVirtualSpace(int selection) {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_GETSELECTIONNCARETVIRTUALSPACE, (WPARAM)selection, (LPARAM)0);
	}

	/// Set the virtual space of the anchor of the nth selection.
	void SetSelectionNAnchorVirtualSpace(int selection, int space) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SETSELECTIONNANCHORVIRTUALSPACE, (WPARAM)selection, (LPARAM)space);
	}

	/// Return the virtual space of the anchor of the nth selection.
	int GetSelectionNAnchorVirtualSpace(int selection) {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_GETSELECTIONNANCHORVIRTUALSPACE, (WPARAM)selection, (LPARAM)0);
	}

	/// Sets the position that starts the selection - this becomes the anchor.
	void SetSelectionNStart(int selection, int anchor) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SETSELECTIONNSTART, (WPARAM)selection, (LPARAM)anchor);
	}

	/// Returns the position at the start of the selection.
	int GetSelectionNStart(int selection) {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_GETSELECTIONNSTART, (WPARAM)selection, (LPARAM)0);
	}

	/// Returns the virtual space at the start of the selection.
	int GetSelectionNStartVirtualSpace(int selection) {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_GETSELECTIONNSTARTVIRTUALSPACE, (WPARAM)selection, (LPARAM)0);
	}

	/// Sets the position that ends the selection - this becomes the currentPosition.
	void SetSelectionNEnd(int selection, int caret) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SETSELECTIONNEND, (WPARAM)selection, (LPARAM)caret);
	}

	/// Returns the virtual space at the end of the selection.
	int GetSelectionNEndVirtualSpace(int selection) {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_GETSELECTIONNENDVIRTUALSPACE, (WPARAM)selection, (LPARAM)0);
	}

	/// Returns the position at the end of the selection.
	int GetSelectionNEnd(int selection) {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_GETSELECTIONNEND, (WPARAM)selection, (LPARAM)0);
	}

	/// Set the caret position of the rectangular selection.
	void SetRectangularSelectionCaret(int caret) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SETRECTANGULARSELECTIONCARET, (WPARAM)caret, (LPARAM)0);
	}

	/// Return the caret position of the rectangular selection.
	int GetRectangularSelectionCaret() {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_GETRECTANGULARSELECTIONCARET, (WPARAM)0, (LPARAM)0);
	}

	/// Set the anchor position of the rectangular selection.
	void SetRectangularSelectionAnchor(int anchor) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SETRECTANGULARSELECTIONANCHOR, (WPARAM)anchor, (LPARAM)0);
	}

	/// Return the anchor position of the rectangular selection.
	int GetRectangularSelectionAnchor() {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_GETRECTANGULARSELECTIONANCHOR, (WPARAM)0, (LPARAM)0);
	}

	/// Set the virtual space of the caret of the rectangular selection.
	void SetRectangularSelectionCaretVirtualSpace(int space) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SETRECTANGULARSELECTIONCARETVIRTUALSPACE, (WPARAM)space, (LPARAM)0);
	}

	/// Return the virtual space of the caret of the rectangular selection.
	int GetRectangularSelectionCaretVirtualSpace() {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_GETRECTANGULARSELECTIONCARETVIRTUALSPACE, (WPARAM)0, (LPARAM)0);
	}

	/// Set the virtual space of the anchor of the rectangular selection.
	void SetRectangularSelectionAnchorVirtualSpace(int space) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SETRECTANGULARSELECTIONANCHORVIRTUALSPACE, (WPARAM)space, (LPARAM)0);
	}

	/// Return the virtual space of the anchor of the rectangular selection.
	int GetRectangularSelectionAnchorVirtualSpace() {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_GETRECTANGULARSELECTIONANCHORVIRTUALSPACE, (WPARAM)0, (LPARAM)0);
	}

	/// Set options for virtual space behaviour.
	void SetVirtualSpaceOptions(int virtualSpaceOptions) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SETVIRTUALSPACEOPTIONS, (WPARAM)virtualSpaceOptions, (LPARAM)0);
	}

	/// Return options for virtual space behaviour.
	int GetVirtualSpaceOptions() {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_GETVIRTUALSPACEOPTIONS, (WPARAM)0, (LPARAM)0);
	}

	/// On GTK, allow selecting the modifier key to use for mouse-based
	/// rectangular selection. Often the window manager requires Alt+Mouse Drag
	/// for moving windows.
	/// Valid values are SCMOD_CTRL(default), SCMOD_ALT, or SCMOD_SUPER.
	void SetRectangularSelectionModifier(int modifier) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SETRECTANGULARSELECTIONMODIFIER, (WPARAM)modifier, (LPARAM)0);
	}

	/// Get the modifier key used for rectangular selection.
	int GetRectangularSelectionModifier() {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_GETRECTANGULARSELECTIONMODIFIER, (WPARAM)0, (LPARAM)0);
	}

	/// Set the foreground colour of additional selections.
	/// Must have previously called SetSelFore with non-zero first argument for this to have an effect.
	void SetAdditionalSelFore(COLORREF fore) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SETADDITIONALSELFORE, (WPARAM)fore, (LPARAM)0);
	}

	/// Set the background colour of additional selections.
	/// Must have previously called SetSelBack with non-zero first argument for this to have an effect.
	void SetAdditionalSelBack(COLORREF back) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SETADDITIONALSELBACK, (WPARAM)back, (LPARAM)0);
	}

	/// Set the alpha of the selection.
	void SetAdditionalSelAlpha(int alpha) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SETADDITIONALSELALPHA, (WPARAM)alpha, (LPARAM)0);
	}

	/// Get the alpha of the selection.
	int GetAdditionalSelAlpha() {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_GETADDITIONALSELALPHA, (WPARAM)0, (LPARAM)0);
	}

	/// Set the foreground colour of additional carets.
	void SetAdditionalCaretFore(COLORREF fore) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SETADDITIONALCARETFORE, (WPARAM)fore, (LPARAM)0);
	}

	/// Get the foreground colour of additional carets.
	COLORREF GetAdditionalCaretFore() {
		ATLASSERT(::IsWindow(m_hWnd));
		return (COLORREF)::SendMessage(m_hWnd, SCI_GETADDITIONALCARETFORE, (WPARAM)0, (LPARAM)0);
	}

	/// Set the main selection to the next selection.
	void RotateSelection() {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_ROTATESELECTION, (WPARAM)0, (LPARAM)0);
	}

	/// Swap that caret and anchor of the main selection.
	void SwapMainAnchorCaret() {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SWAPMAINANCHORCARET, (WPARAM)0, (LPARAM)0);
	}

	/// Add the next occurrence of the main selection to the set of selections as main.
	/// If the current selection is empty then select word around caret.
	void MultipleSelectAddNext() {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_MULTIPLESELECTADDNEXT, (WPARAM)0, (LPARAM)0);
	}

	/// Add each occurrence of the main selection in the target to the set of selections.
	/// If the current selection is empty then select word around caret.
	void MultipleSelectAddEach() {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_MULTIPLESELECTADDEACH, (WPARAM)0, (LPARAM)0);
	}

	/// Indicate that the internal state of a lexer has changed over a range and therefore
	/// there may be a need to redraw.
	int ChangeLexerState(int start, int end) {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_CHANGELEXERSTATE, (WPARAM)start, (LPARAM)end);
	}

	/// Find the next line at or after lineStart that is a contracted fold header line.
	/// Return -1 when no more lines.
	int ContractedFoldNext(int lineStart) {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_CONTRACTEDFOLDNEXT, (WPARAM)lineStart, (LPARAM)0);
	}

	/// Centre current line in window.
	void VerticalCentreCaret() {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_VERTICALCENTRECARET, (WPARAM)0, (LPARAM)0);
	}

	/// Move the selected lines up one line, shifting the line above after the selection
	void MoveSelectedLinesUp() {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_MOVESELECTEDLINESUP, (WPARAM)0, (LPARAM)0);
	}

	/// Move the selected lines down one line, shifting the line below before the selection
	void MoveSelectedLinesDown() {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_MOVESELECTEDLINESDOWN, (WPARAM)0, (LPARAM)0);
	}

	/// Set the identifier reported as idFrom in notification messages.
	void SetIdentifier(int identifier) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SETIDENTIFIER, (WPARAM)identifier, (LPARAM)0);
	}

	/// Get the identifier.
	int GetIdentifier() {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_GETIDENTIFIER, (WPARAM)0, (LPARAM)0);
	}

	/// Set the width for future RGBA image data.
	void RGBAImageSetWidth(int width) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_RGBAIMAGESETWIDTH, (WPARAM)width, (LPARAM)0);
	}

	/// Set the height for future RGBA image data.
	void RGBAImageSetHeight(int height) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_RGBAIMAGESETHEIGHT, (WPARAM)height, (LPARAM)0);
	}

	/// Set the scale factor in percent for future RGBA image data.
	void RGBAImageSetScale(int scalePercent) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_RGBAIMAGESETSCALE, (WPARAM)scalePercent, (LPARAM)0);
	}

	/// Define a marker from RGBA data.
	/// It has the width and height from RGBAImageSetWidth/Height
	void MarkerDefineRGBAImage(int markerNumber, LPCSTR pixels) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_MARKERDEFINERGBAIMAGE, (WPARAM)markerNumber, (LPARAM)pixels);
	}

	/// Register an RGBA image for use in autocompletion lists.
	/// It has the width and height from RGBAImageSetWidth/Height
	void RegisterRGBAImage(int type, LPCSTR pixels) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_REGISTERRGBAIMAGE, (WPARAM)type, (LPARAM)pixels);
	}

	/// Scroll to start of document.
	void ScrollToStart() {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SCROLLTOSTART, (WPARAM)0, (LPARAM)0);
	}

	/// Scroll to end of document.
	void ScrollToEnd() {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SCROLLTOEND, (WPARAM)0, (LPARAM)0);
	}

	/// Set the technology used.
	void SetTechnology(int technology) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SETTECHNOLOGY, (WPARAM)technology, (LPARAM)0);
	}

	/// Get the tech.
	int GetTechnology() {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_GETTECHNOLOGY, (WPARAM)0, (LPARAM)0);
	}

	/// Create an ILoader*.
	void* CreateLoader(int bytes, int documentOptions) {
		ATLASSERT(::IsWindow(m_hWnd));
		return (void*)::SendMessage(m_hWnd, SCI_CREATELOADER, (WPARAM)bytes, (LPARAM)documentOptions);
	}

	/// On macOS, show a find indicator.
	void FindIndicatorShow(int start, int end) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_FINDINDICATORSHOW, (WPARAM)start, (LPARAM)end);
	}

	/// On macOS, flash a find indicator, then fade out.
	void FindIndicatorFlash(int start, int end) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_FINDINDICATORFLASH, (WPARAM)start, (LPARAM)end);
	}

	/// On macOS, hide the find indicator.
	void FindIndicatorHide() {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_FINDINDICATORHIDE, (WPARAM)0, (LPARAM)0);
	}

	/// Move caret to before first visible character on display line.
	/// If already there move to first character on display line.
	void VCHomeDisplay() {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_VCHOMEDISPLAY, (WPARAM)0, (LPARAM)0);
	}

	/// Like VCHomeDisplay but extending selection to new caret position.
	void VCHomeDisplayExtend() {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_VCHOMEDISPLAYEXTEND, (WPARAM)0, (LPARAM)0);
	}

	/// Is the caret line always visible?
	bool GetCaretLineVisibleAlways() {
		ATLASSERT(::IsWindow(m_hWnd));
		return ::SendMessage(m_hWnd, SCI_GETCARETLINEVISIBLEALWAYS, (WPARAM)0, (LPARAM)0) != 0;
	}

	/// Sets the caret line to always visible.
	void SetCaretLineVisibleAlways(bool alwaysVisible) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SETCARETLINEVISIBLEALWAYS, (WPARAM)alwaysVisible, (LPARAM)0);
	}

	/// Set the line end types that the application wants to use. May not be used if incompatible with lexer or encoding.
	void SetLineEndTypesAllowed(int lineEndBitSet) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SETLINEENDTYPESALLOWED, (WPARAM)lineEndBitSet, (LPARAM)0);
	}

	/// Get the line end types currently allowed.
	int GetLineEndTypesAllowed() {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_GETLINEENDTYPESALLOWED, (WPARAM)0, (LPARAM)0);
	}

	/// Get the line end types currently recognised. May be a subset of the allowed types due to lexer limitation.
	int GetLineEndTypesActive() {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_GETLINEENDTYPESACTIVE, (WPARAM)0, (LPARAM)0);
	}

	/// Set the way a character is drawn.
	void SetRepresentation(LPCSTR encodedCharacter, LPCSTR representation) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SETREPRESENTATION, (WPARAM)encodedCharacter, (LPARAM)representation);
	}

	/// Get the way a character is drawn.
	/// Result is NUL-terminated.
	int GetRepresentation(LPCSTR encodedCharacter, LPSTR representation) {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_GETREPRESENTATION, (WPARAM)encodedCharacter, (LPARAM)representation);
	}

	/// Remove a character representation.
	void ClearRepresentation(LPCSTR encodedCharacter) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_CLEARREPRESENTATION, (WPARAM)encodedCharacter, (LPARAM)0);
	}

	/// Clear representations to default.
	void ClearAllRepresentations() {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_CLEARALLREPRESENTATIONS, (WPARAM)0, (LPARAM)0);
	}

	/// Set the appearance of a representation.
	void SetRepresentationAppearance(LPCSTR encodedCharacter, int appearance) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SETREPRESENTATIONAPPEARANCE, (WPARAM)encodedCharacter, (LPARAM)appearance);
	}

	/// Get the appearance of a representation.
	int GetRepresentationAppearance(LPCSTR encodedCharacter) {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_GETREPRESENTATIONAPPEARANCE, (WPARAM)encodedCharacter, (LPARAM)0);
	}

	/// Set the colour of a representation.
	void SetRepresentationColour(LPCSTR encodedCharacter, COLORREF colour) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SETREPRESENTATIONCOLOUR, (WPARAM)encodedCharacter, (LPARAM)colour);
	}

	/// Get the colour of a representation.
	COLORREF GetRepresentationColour(LPCSTR encodedCharacter) {
		ATLASSERT(::IsWindow(m_hWnd));
		return (COLORREF)::SendMessage(m_hWnd, SCI_GETREPRESENTATIONCOLOUR, (WPARAM)encodedCharacter, (LPARAM)0);
	}

	/// Set the end of line annotation text for a line
	void EOLAnnotationSetText(int line, LPCSTR text) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_EOLANNOTATIONSETTEXT, (WPARAM)line, (LPARAM)text);
	}

	/// Get the end of line annotation text for a line
	int EOLAnnotationGetText(int line, LPSTR text) {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_EOLANNOTATIONGETTEXT, (WPARAM)line, (LPARAM)text);
	}

	/// Set the style number for the end of line annotations for a line
	void EOLAnnotationSetStyle(int line, int style) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_EOLANNOTATIONSETSTYLE, (WPARAM)line, (LPARAM)style);
	}

	/// Get the style number for the end of line annotations for a line
	int EOLAnnotationGetStyle(int line) {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_EOLANNOTATIONGETSTYLE, (WPARAM)line, (LPARAM)0);
	}

	/// Clear the end of annotations from all lines
	void EOLAnnotationClearAll() {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_EOLANNOTATIONCLEARALL, (WPARAM)0, (LPARAM)0);
	}

	/// Set the visibility for the end of line annotations for a view
	void EOLAnnotationSetVisible(int visible) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_EOLANNOTATIONSETVISIBLE, (WPARAM)visible, (LPARAM)0);
	}

	/// Get the visibility for the end of line annotations for a view
	int EOLAnnotationGetVisible() {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_EOLANNOTATIONGETVISIBLE, (WPARAM)0, (LPARAM)0);
	}

	/// Set the start of the range of style numbers used for end of line annotations
	void EOLAnnotationSetStyleOffset(int style) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_EOLANNOTATIONSETSTYLEOFFSET, (WPARAM)style, (LPARAM)0);
	}

	/// Get the start of the range of style numbers used for end of line annotations
	int EOLAnnotationGetStyleOffset() {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_EOLANNOTATIONGETSTYLEOFFSET, (WPARAM)0, (LPARAM)0);
	}

	/// Get whether a feature is supported
	bool SupportsFeature(int feature) {
		ATLASSERT(::IsWindow(m_hWnd));
		return ::SendMessage(m_hWnd, SCI_SUPPORTSFEATURE, (WPARAM)feature, (LPARAM)0) != 0;
	}

	/// Retrieve line character index state.
	int GetLineCharacterIndex() {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_GETLINECHARACTERINDEX, (WPARAM)0, (LPARAM)0);
	}

	/// Request line character index be created or its use count increased.
	void AllocateLineCharacterIndex(int lineCharacterIndex) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_ALLOCATELINECHARACTERINDEX, (WPARAM)lineCharacterIndex, (LPARAM)0);
	}

	/// Decrease use count of line character index and remove if 0.
	void ReleaseLineCharacterIndex(int lineCharacterIndex) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_RELEASELINECHARACTERINDEX, (WPARAM)lineCharacterIndex, (LPARAM)0);
	}

	/// Retrieve the document line containing a position measured in index units.
	int LineFromIndexPosition(int pos, int lineCharacterIndex) {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_LINEFROMINDEXPOSITION, (WPARAM)pos, (LPARAM)lineCharacterIndex);
	}

	/// Retrieve the position measured in index units at the start of a document line.
	int IndexPositionFromLine(int line, int lineCharacterIndex) {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_INDEXPOSITIONFROMLINE, (WPARAM)line, (LPARAM)lineCharacterIndex);
	}

	/// Start notifying the container of all key presses and commands.
	void StartRecord() {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_STARTRECORD, (WPARAM)0, (LPARAM)0);
	}

	/// Stop notifying the container of all key presses and commands.
	void StopRecord() {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_STOPRECORD, (WPARAM)0, (LPARAM)0);
	}

	/// Retrieve the lexing language of the document.
	int GetLexer() {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_GETLEXER, (WPARAM)0, (LPARAM)0);
	}

	/// Colourise a segment of the document using the current lexing language.
	void Colourise(int start, int end) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_COLOURISE, (WPARAM)start, (LPARAM)end);
	}

	/// Set up a value that may be used by a lexer for some optional feature.
	void SetProperty(LPCSTR key, LPCSTR value) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SETPROPERTY, (WPARAM)key, (LPARAM)value);
	}

	/// Set up the key words used by the lexer.
	void SetKeyWords(int keyWordSet, LPCSTR keyWords) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SETKEYWORDS, (WPARAM)keyWordSet, (LPARAM)keyWords);
	}

	/// Retrieve a "property" value previously set with SetProperty.
	/// Result is NUL-terminated.
	int GetProperty(LPCSTR key, LPSTR value) {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_GETPROPERTY, (WPARAM)key, (LPARAM)value);
	}

	/// Retrieve a "property" value previously set with SetProperty,
	/// with "$()" variable replacement on returned buffer.
	/// Result is NUL-terminated.
	int GetPropertyExpanded(LPCSTR key, LPSTR value) {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_GETPROPERTYEXPANDED, (WPARAM)key, (LPARAM)value);
	}

	/// Retrieve a "property" value previously set with SetProperty,
	/// interpreted as an int AFTER any "$()" variable replacement.
	int GetPropertyInt(LPCSTR key, int defaultValue) {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_GETPROPERTYINT, (WPARAM)key, (LPARAM)defaultValue);
	}

	/// Retrieve the name of the lexer.
	/// Return the length of the text.
	/// Result is NUL-terminated.
	int GetLexerLanguage(LPSTR language) {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_GETLEXERLANGUAGE, (WPARAM)0, (LPARAM)language);
	}

	/// For private communication between an application and a known lexer.
	void* PrivateLexerCall(int operation, void* pointer) {
		ATLASSERT(::IsWindow(m_hWnd));
		return (void*)::SendMessage(m_hWnd, SCI_PRIVATELEXERCALL, (WPARAM)operation, (LPARAM)pointer);
	}

	/// Retrieve a '\n' separated list of properties understood by the current lexer.
	/// Result is NUL-terminated.
	int PropertyNames(LPSTR names) {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_PROPERTYNAMES, (WPARAM)0, (LPARAM)names);
	}

	/// Retrieve the type of a property.
	int PropertyType(LPCSTR name) {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_PROPERTYTYPE, (WPARAM)name, (LPARAM)0);
	}

	/// Describe a property.
	/// Result is NUL-terminated.
	int DescribeProperty(LPCSTR name, LPSTR description) {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_DESCRIBEPROPERTY, (WPARAM)name, (LPARAM)description);
	}

	/// Retrieve a '\n' separated list of descriptions of the keyword sets understood by the current lexer.
	/// Result is NUL-terminated.
	int DescribeKeyWordSets(LPSTR descriptions) {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_DESCRIBEKEYWORDSETS, (WPARAM)0, (LPARAM)descriptions);
	}

	/// Bit set of LineEndType enumertion for which line ends beyond the standard
	/// LF, CR, and CRLF are supported by the lexer.
	int GetLineEndTypesSupported() {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_GETLINEENDTYPESSUPPORTED, (WPARAM)0, (LPARAM)0);
	}

	/// Allocate a set of sub styles for a particular base style, returning start of range
	int AllocateSubStyles(int styleBase, int numberStyles) {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_ALLOCATESUBSTYLES, (WPARAM)styleBase, (LPARAM)numberStyles);
	}

	/// The starting style number for the sub styles associated with a base style
	int GetSubStylesStart(int styleBase) {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_GETSUBSTYLESSTART, (WPARAM)styleBase, (LPARAM)0);
	}

	/// The number of sub styles associated with a base style
	int GetSubStylesLength(int styleBase) {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_GETSUBSTYLESLENGTH, (WPARAM)styleBase, (LPARAM)0);
	}

	/// For a sub style, return the base style, else return the argument.
	int GetStyleFromSubStyle(int subStyle) {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_GETSTYLEFROMSUBSTYLE, (WPARAM)subStyle, (LPARAM)0);
	}

	/// For a secondary style, return the primary style, else return the argument.
	int GetPrimaryStyleFromStyle(int style) {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_GETPRIMARYSTYLEFROMSTYLE, (WPARAM)style, (LPARAM)0);
	}

	/// Free allocated sub styles
	void FreeSubStyles() {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_FREESUBSTYLES, (WPARAM)0, (LPARAM)0);
	}

	/// Set the identifiers that are shown in a particular style
	void SetIdentifiers(int style, LPCSTR identifiers) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SETIDENTIFIERS, (WPARAM)style, (LPARAM)identifiers);
	}

	/// Where styles are duplicated by a feature such as active/inactive code
	/// return the distance between the two types.
	int DistanceToSecondaryStyles() {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_DISTANCETOSECONDARYSTYLES, (WPARAM)0, (LPARAM)0);
	}

	/// Get the set of base styles that can be extended with sub styles
	/// Result is NUL-terminated.
	int GetSubStyleBases(LPSTR styles) {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_GETSUBSTYLEBASES, (WPARAM)0, (LPARAM)styles);
	}

	/// Retrieve the number of named styles for the lexer.
	int GetNamedStyles() {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_GETNAMEDSTYLES, (WPARAM)0, (LPARAM)0);
	}

	/// Retrieve the name of a style.
	/// Result is NUL-terminated.
	int NameOfStyle(int style, LPSTR name) {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_NAMEOFSTYLE, (WPARAM)style, (LPARAM)name);
	}

	/// Retrieve a ' ' separated list of style tags like "literal quoted string".
	/// Result is NUL-terminated.
	int TagsOfStyle(int style, LPSTR tags) {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_TAGSOFSTYLE, (WPARAM)style, (LPARAM)tags);
	}

	/// Retrieve a description of a style.
	/// Result is NUL-terminated.
	int DescriptionOfStyle(int style, LPSTR description) {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_DESCRIPTIONOFSTYLE, (WPARAM)style, (LPARAM)description);
	}

	/// Set the lexer from an ILexer*.
	void SetILexer(void* ilexer) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SETILEXER, (WPARAM)0, (LPARAM)ilexer);
	}

	/// Retrieve bidirectional text display state.
	int GetBidirectional() {
		ATLASSERT(::IsWindow(m_hWnd));
		return (int)::SendMessage(m_hWnd, SCI_GETBIDIRECTIONAL, (WPARAM)0, (LPARAM)0);
	}

	/// Set bidirectional text display state.
	void SetBidirectional(int bidirectional) {
		ATLASSERT(::IsWindow(m_hWnd));
		::SendMessage(m_hWnd, SCI_SETBIDIRECTIONAL, (WPARAM)bidirectional, (LPARAM)0);
	}
	/* --Autogenerated -- end of section automatically generated from Scintilla.iface */
};

typedef CScintillaT<CWindow>		CScintilla;

}; //namespace WTL
